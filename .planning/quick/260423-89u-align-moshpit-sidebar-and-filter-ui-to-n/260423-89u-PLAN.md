---
phase: 260423-89u-align-moshpit-sidebar
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/moshpit/components/MoshpitSideRail.vue
  - src/platform/moshpit/components/MoshpitSettingsPanel.vue
  - src/platform/moshpit/components/MoshpitAddFilterPopover.vue
  - src/platform/moshpit/components/MoshpitWorkflowPicker.vue
  - src/platform/moshpit/components/MoshpitGroupingToggles.vue
  - src/platform/moshpit/components/MoshpitShowHiddenToggle.vue
  - src/platform/moshpit/components/MoshpitTimeRangePicker.vue
  - src/platform/moshpit/components/MoshpitFilterChipRow.vue
autonomous: true
requirements:
  - MOSHPIT-STYLE-ALIGNMENT
must_haves:
  truths:
    - 'All four Moshpit side-panel surfaces use bg-comfy-menu-bg (matches native RightSidePanel).'
    - 'Active-state toggles in MoshpitGroupingToggles and MoshpitShowHiddenToggle use bg-interface-menu-component-surface-selected (matches native toggleGroup).'
    - 'Filter chip and time-range preset buttons render via the shared Button component (size=sm, variant=secondary).'
    - 'Input/chip borders use border-border-default (no stale border-border-subtle on filter editors after consolidation).'
    - 'pnpm typecheck and pnpm lint pass after every task.'
  artifacts:
    - path: 'src/platform/moshpit/components/MoshpitSideRail.vue'
      provides: 'Rail surface on bg-comfy-menu-bg.'
    - path: 'src/platform/moshpit/components/MoshpitSettingsPanel.vue'
      provides: 'Settings panel surface on bg-comfy-menu-bg.'
    - path: 'src/platform/moshpit/components/MoshpitAddFilterPopover.vue'
      provides: 'Popover surface on bg-comfy-menu-bg.'
    - path: 'src/platform/moshpit/components/MoshpitWorkflowPicker.vue'
      provides: 'Combobox popover surface on bg-comfy-menu-bg.'
    - path: 'src/platform/moshpit/components/MoshpitGroupingToggles.vue'
      provides: 'Active pill uses bg-interface-menu-component-surface-selected.'
    - path: 'src/platform/moshpit/components/MoshpitShowHiddenToggle.vue'
      provides: 'Toggle on-state uses bg-interface-menu-component-surface-selected.'
    - path: 'src/platform/moshpit/components/MoshpitTimeRangePicker.vue'
      provides: 'Preset buttons rendered via shared Button component.'
    - path: 'src/platform/moshpit/components/MoshpitFilterChipRow.vue'
      provides: 'Chip remove control rendered via shared Button component.'
  key_links:
    - from: 'MoshpitTimeRangePicker.vue + MoshpitFilterChipRow.vue'
      to: 'src/components/ui/button/Button.vue'
      via: "import Button from '@/components/ui/button/Button.vue'"
      pattern: "from '@/components/ui/button/Button.vue'"
---

<objective>
Close four styling gaps between Moshpit side-panel UI and native ComfyUI:
1. Panel surfaces align to `bg-comfy-menu-bg`.
2. Selected/active states align to `bg-interface-menu-component-surface-selected`.
3. Panel edges + filter-editor borders are standardised.
4. Hand-coded buttons/chips in filter UI are replaced with the shared `Button` component.

Purpose: Restore visual parity with the rest of the ComfyUI shell so Moshpit feels like native surface, not a bolted-on workspace. Small-caps section headers (`text-2xs uppercase tracking-wide`) stay — they are deliberate Moshpit identity.

Output: Four atomic commits (one per category), each with `style(moshpit):` or `fix(moshpit):` prefix consistent with recent `moshpit-tournament` commit style. No tests added — pure styling changes verified via typecheck, lint, and grep for removed tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Shared Button component signature — use this instead of hand-coded buttons in Tasks 4. -->

From src/components/ui/button/Button.vue:

```ts
interface Props extends PrimitiveProps {
  variant?: ButtonVariants['variant'] // 'secondary' (default) recommended here
  size?: ButtonVariants['size'] // 'sm' = 'h-6 rounded-sm px-2 py-1 text-xs' (matches current hand-coded pill dims)
  class?: HTMLAttributes['class'] // use to add selected-state override (e.g. active preset)
  loading?: boolean
  disabled?: boolean
}
```

Usage pattern (from existing codebase):

```vue
<script setup lang="ts">
import Button from '@/components/ui/button/Button.vue'
</script>

<template>
  <Button
    size="sm"
    variant="secondary"
    :class="
      cn(
        isActive &&
          'bg-interface-menu-component-surface-selected text-base-foreground'
      )
    "
    @click="..."
  >
    Label
  </Button>
</template>
```

Notes:

- `size="sm"` produces `h-6 rounded-sm px-2 py-1 text-xs` — same height and padding as the existing hand-coded pills in MoshpitTimeRangePicker / MoshpitFilterChipRow.
- Recent `moshpit-tournament` commits use `style(moshpit-tournament):` and `fix(moshpit-tournament):` — this plan uses `style(moshpit):` / `fix(moshpit):` for the generic moshpit scope.
  </interfaces>

<executor_reminders>

- **NEVER** use `any` or `as any`. Pure styling — no type changes expected.
- **NEVER** use the `dark:` variant. All four files already comply; preserve that.
- **NEVER** use `:class="[]"` to merge classes — always `cn()` from `@/utils/tailwindUtil`.
- **NEVER** use `!important` / `!` prefix.
- **NEVER** use `--no-verify` on commits. Fix root cause if a hook fails.
- Run `pnpm typecheck` and `pnpm lint` after each task. They must pass before committing.
- Commit after each task with a focused `style(moshpit):` / `fix(moshpit):` message. Do NOT mention Claude/AI in commits.
- Out of scope (do NOT change): small-caps section headers (`text-2xs uppercase tracking-wide`).
  </executor_reminders>
  </context>

<tasks>

<task type="auto">
  <name>Task 1: Align panel surfaces to bg-comfy-menu-bg</name>
  <files>
    src/platform/moshpit/components/MoshpitSideRail.vue,
    src/platform/moshpit/components/MoshpitSettingsPanel.vue,
    src/platform/moshpit/components/MoshpitAddFilterPopover.vue,
    src/platform/moshpit/components/MoshpitWorkflowPicker.vue
  </files>
  <action>
Replace the surface tokens on the four panel containers so they match the native `RightSidePanel` (`bg-comfy-menu-bg`).

1. `src/platform/moshpit/components/MoshpitSideRail.vue` — on the `<nav>` root (around line 4), change `bg-node-component-surface` → `bg-comfy-menu-bg`. Keep `border-r border-(--interface-stroke)`.

2. `src/platform/moshpit/components/MoshpitSettingsPanel.vue` — on the `<aside>` root (around line 4), change `bg-node-component-surface` → `bg-comfy-menu-bg`. Keep `border-r border-(--interface-stroke)`.

3. `src/platform/moshpit/components/MoshpitAddFilterPopover.vue` — inside the `PopoverContent` `cn(...)` block (around line 21), change `bg-interface-panel-surface` → `bg-comfy-menu-bg`. Keep the border + shadow + animation classes.

4. `src/platform/moshpit/components/MoshpitWorkflowPicker.vue` — inside the `ComboboxContent` `cn(...)` block (around line 49), change `bg-interface-panel-surface` → `bg-comfy-menu-bg`. Keep the border + shadow classes.

Border verification (panel edges, Category 3 first half — panels only):

- `MoshpitSideRail.vue` root already uses `border-r border-(--interface-stroke)` — verify, leave unchanged.
- `MoshpitSettingsPanel.vue` root already uses `border-r border-(--interface-stroke)` — verify, leave unchanged.
- `MoshpitAddFilterPopover.vue` `PopoverContent` uses `border border-border-subtle` — LEAVE this one alone in Task 1; panel-edge consolidation (Category 3) is only for `border-(--interface-stroke)` on the outer panel shells. Input/chip edge consolidation comes in Task 3.
- `MoshpitWorkflowPicker.vue` `ComboboxContent` uses `border border-border-subtle` — same; LEAVE alone in Task 1.

Do NOT touch: small-caps section headers, internal chip/pill styling, button sizing — those belong to later tasks.
</action>
<verify>
<automated> # Old tokens must be gone from the four files
! grep -n 'bg-node-component-surface' src/platform/moshpit/components/MoshpitSideRail.vue src/platform/moshpit/components/MoshpitSettingsPanel.vue \
 && ! grep -n 'bg-interface-panel-surface' src/platform/moshpit/components/MoshpitAddFilterPopover.vue src/platform/moshpit/components/MoshpitWorkflowPicker.vue \
 && grep -qn 'bg-comfy-menu-bg' src/platform/moshpit/components/MoshpitSideRail.vue \
 && grep -qn 'bg-comfy-menu-bg' src/platform/moshpit/components/MoshpitSettingsPanel.vue \
 && grep -qn 'bg-comfy-menu-bg' src/platform/moshpit/components/MoshpitAddFilterPopover.vue \
 && grep -qn 'bg-comfy-menu-bg' src/platform/moshpit/components/MoshpitWorkflowPicker.vue \
 && pnpm typecheck \
 && pnpm lint
</automated>
</verify>
<done>
All four panel surfaces use `bg-comfy-menu-bg`. Typecheck + lint green. Commit message: `style(moshpit): align panel surfaces to bg-comfy-menu-bg`.
</done>
</task>

<task type="auto">
  <name>Task 2: Align selection states to bg-interface-menu-component-surface-selected</name>
  <files>
    src/platform/moshpit/components/MoshpitGroupingToggles.vue,
    src/platform/moshpit/components/MoshpitShowHiddenToggle.vue
  </files>
  <action>
Replace bespoke active-state tokens so active/selected controls match the native `toggleGroup` treatment.

1. `src/platform/moshpit/components/MoshpitGroupingToggles.vue` (around line 24):
   - In the active-arm of the `cn(...)` class binding, replace `bg-node-component-primary text-base-foreground` with `bg-interface-menu-component-surface-selected text-base-foreground`.
   - Leave the inactive arm (`border border-(--interface-stroke) text-muted-foreground hover:text-base-foreground`) untouched.

2. `src/platform/moshpit/components/MoshpitShowHiddenToggle.vue` (around line 15):
   - In the ternary that picks the switch background, replace the on-state `bg-primary-background` with `bg-interface-menu-component-surface-selected`.
   - Keep the off-state `bg-secondary-background`, the focus-visible ring, and the thumb sub-element completely unchanged.

Do not change any other styling in these files (transition, ring, layout). Do not touch the switch thumb's `bg-base-background`.
</action>
<verify>
<automated> # Old active-state tokens must be gone from these files
! grep -n 'bg-node-component-primary' src/platform/moshpit/components/MoshpitGroupingToggles.vue \
 && ! grep -n 'bg-primary-background' src/platform/moshpit/components/MoshpitShowHiddenToggle.vue \
 && grep -qn 'bg-interface-menu-component-surface-selected' src/platform/moshpit/components/MoshpitGroupingToggles.vue \
 && grep -qn 'bg-interface-menu-component-surface-selected' src/platform/moshpit/components/MoshpitShowHiddenToggle.vue \
 && pnpm typecheck \
 && pnpm lint
</automated>
</verify>
<done>
Both selection states use `bg-interface-menu-component-surface-selected`. Typecheck + lint green. Commit message: `style(moshpit): align selection states to native toggle token`.
</done>
</task>

<task type="auto">
  <name>Task 3: Standardise input and chip borders to border-border-default</name>
  <files>
    src/platform/moshpit/components/MoshpitAddFilterPopover.vue,
    src/platform/moshpit/components/MoshpitWorkflowPicker.vue,
    src/platform/moshpit/components/MoshpitTimeRangePicker.vue
  </files>
  <action>
Consolidate filter-editor / input / popover-content borders from `border-border-subtle` → `border-border-default`. Panel OUTER edges (`border-(--interface-stroke)` on the side rail + settings panel) were already correct in Task 1 — do not touch them.

1. `src/platform/moshpit/components/MoshpitAddFilterPopover.vue`:
   - `PopoverContent` cn block (around line 21): `border border-border-subtle` → `border border-border-default`.
   - Step 1 search input (around line 32): `border-b border-border-subtle` → `border-b border-border-default`.
   - `triggerClass` computed (around lines 206–215): in BOTH the `variant === 'block'` and the `'inline'` branches, change `border border-border-subtle` → `border border-border-default`. Leave the `hover:border-border-default` hover class exactly as-is (it is already correct and happens to now equal the base — that is intended).

2. `src/platform/moshpit/components/MoshpitWorkflowPicker.vue`:
   - Anchor button cn block (around line 15): `border border-border-subtle` → `border border-border-default`.
   - `ComboboxContent` cn block (around line 49): `border border-border-subtle` → `border border-border-default`.
   - `ComboboxInput` sticky search (around line 55): `border-b border-border-subtle` → `border-b border-border-default`.

3. `src/platform/moshpit/components/MoshpitTimeRangePicker.vue`:
   - Both date `<input type="date">` elements (around lines 46 and 61): `border border-border-subtle` → `border border-border-default`.

Scope check:

- Do NOT change `border-(--interface-stroke)` on `MoshpitSideRail.vue` / `MoshpitSettingsPanel.vue` / `MoshpitGroupingToggles.vue`. Those are panel-edge strokes and were already correct per audit.
- Do NOT touch any file outside the three listed above.
  </action>
  <verify>
  <automated> # border-border-subtle must be gone from the three touched files
  ! grep -n 'border-border-subtle' src/platform/moshpit/components/MoshpitAddFilterPopover.vue \
   && ! grep -n 'border-border-subtle' src/platform/moshpit/components/MoshpitWorkflowPicker.vue \
   && ! grep -n 'border-border-subtle' src/platform/moshpit/components/MoshpitTimeRangePicker.vue \
   && grep -qn 'border-border-default' src/platform/moshpit/components/MoshpitAddFilterPopover.vue \
   && grep -qn 'border-border-default' src/platform/moshpit/components/MoshpitWorkflowPicker.vue \
   && grep -qn 'border-border-default' src/platform/moshpit/components/MoshpitTimeRangePicker.vue \
   && pnpm typecheck \
   && pnpm lint
  </automated>
  </verify>
  <done>
  All filter-editor / popover input borders use `border-border-default`. Panel outer strokes (`border-(--interface-stroke)`) remain unchanged. Typecheck + lint green. Commit message: `style(moshpit): consolidate filter input borders on border-border-default`.
  </done>
  </task>

<task type="auto">
  <name>Task 4: Replace hand-coded pills/chips with shared Button component</name>
  <files>
    src/platform/moshpit/components/MoshpitTimeRangePicker.vue,
    src/platform/moshpit/components/MoshpitFilterChipRow.vue
  </files>
  <action>
Button component lives at `src/components/ui/button/Button.vue` (import path: `@/components/ui/button/Button.vue`). `size="sm"` maps to `h-6 rounded-sm px-2 py-1 text-xs`, which matches the dimensions of the current hand-coded pills.

1. `src/platform/moshpit/components/MoshpitTimeRangePicker.vue` (around lines 12–29):
   - Add `import Button from '@/components/ui/button/Button.vue'` (group with other `@/` imports in the existing import block, above the vue-i18n / store imports in the sorted order — `pnpm format` will normalise).
   - Replace the `<button>` used in the `v-for="preset in TIME_PRESETS"` loop with `<Button>`.
   - On the `<Button>` element:
     - Preserve `:key="preset"`, `type="button"`, `role="radio"`, `:aria-checked="filterStore.timeRange.preset === preset"`, and `@click="selectPreset(preset)"`.
     - Set `size="sm"` and `variant="secondary"`.
     - Remove the hand-coded `h-6 rounded-full px-2 text-xs` base classes (Button `size="sm"` provides equivalent sizing; the button corner becomes `rounded-sm` by the variant instead of `rounded-full` — that is intentional to match the shared Button's canonical shape).
     - Port the selected/unselected visual delta onto the `:class` via `cn(...)`:
       - Selected: `bg-interface-menu-component-surface-selected text-base-foreground`
       - Unselected: leave Button's default `secondary` styling (no extra class needed), but preserve the existing muted text for unselected presets by adding `text-muted-foreground hover:text-base-foreground` when NOT selected.
     - Example shape:
       ```vue
       <Button
         v-for="preset in TIME_PRESETS"
         :key="preset"
         type="button"
         role="radio"
         size="sm"
         variant="secondary"
         :aria-checked="filterStore.timeRange.preset === preset"
         :class="
           cn(
             filterStore.timeRange.preset === preset
               ? 'bg-interface-menu-component-surface-selected text-base-foreground'
               : 'text-muted-foreground hover:text-base-foreground'
           )
         "
         @click="selectPreset(preset)"
       >
         {{ presetLabel(preset) }}
       </Button>
       ```
   - Keep `cn` import; it is still used.

2. `src/platform/moshpit/components/MoshpitFilterChipRow.vue` (around lines 23–38):
   - Add `import Button from '@/components/ui/button/Button.vue'` alongside the existing `@/utils/tailwindUtil` import.
   - Replace the inline `<button>` remove-control inside each chip with `<Button>`.
   - On the `<Button>` element:
     - Use `variant="textonly"` and `size="icon-sm"` (maps to `size-5 p-0` — fits the chip row; the hand-coded original used `size-6` which was slightly oversized).
     - Preserve `type="button"`, `:aria-label="t('moshpit.filters.removeChip', { param: chipLabel(chip) })"`, and `@click="filterStore.removeChip(chip.id)"`.
     - Port the existing hover + focus-visible styling onto the `:class` via `cn(...)`:
       ```
       cn(
         'text-muted-foreground hover:text-base-foreground',
         'focus-visible:outline-1 focus-visible:outline-offset-2 focus-visible:outline-(--focus-ring)'
       )
       ```
     - Inside the Button, keep the existing icon `<i class="icon-[lucide--x] size-5" aria-hidden="true" />` unchanged (or trim to `size-4` if Button's default icon sizing applies — prefer leaving explicit `size-5` for now to avoid a visible shrink).
   - Note: the outer chip wrapper (`<div class="flex items-center gap-1 rounded-md bg-secondary-background px-2 py-1 text-xs">`) stays hand-coded because it is a layout container, not a button. Only the remove-control is swapped.

After both swaps:

- Scan for unused imports (Button must be used; `cn` still used in both files).
- `pnpm format` to normalise import order, then re-run typecheck + lint.

Do NOT change any tests — none were listed. If any co-located `.test.ts` selects the old `<button>` by tag name, Button renders a `<button>` by default (via `Primitive`) so selectors should continue to resolve. If lint or typecheck flags a test, fix the selector rather than skipping the test.
</action>
<verify>
<automated> # Hand-coded pill class combos must be gone; Button import must be present
! grep -nE 'h-6 rounded-full px-2 text-xs' src/platform/moshpit/components/MoshpitTimeRangePicker.vue \
 && ! grep -nE 'inline-flex size-6 shrink-0 cursor-pointer items-center justify-center' src/platform/moshpit/components/MoshpitFilterChipRow.vue \
 && grep -qn "from '@/components/ui/button/Button.vue'" src/platform/moshpit/components/MoshpitTimeRangePicker.vue \
 && grep -qn "from '@/components/ui/button/Button.vue'" src/platform/moshpit/components/MoshpitFilterChipRow.vue \
 && pnpm format \
 && pnpm typecheck \
 && pnpm lint
</automated>
</verify>
<done>
Both hand-coded pill/chip controls use the shared `Button` component with `size="sm"` (time-range presets) and `size="icon-sm" variant="textonly"` (chip remove). Typecheck + lint + format all green. Commit message: `fix(moshpit): replace hand-coded pills with shared Button`.
</done>
</task>

</tasks>

<threat_model>
Not applicable — pure UI styling changes. No trust boundaries crossed, no new input sinks, no auth surface touched. No STRIDE register required.
</threat_model>

<verification>
Aggregate project checks after all four tasks:

```
pnpm typecheck
pnpm lint
```

Manual smoke (optional, not gating):

- Open Moshpit; verify side rail + settings panel share the same dark surface as the native right-side panel.
- Click a grouping pill and the show-hidden toggle; active/on state uses the interface-menu-component-surface-selected token.
- Open the Add Filter popover and the Workflow Picker; popover surfaces match the settings panel, input edges use border-border-default.
- Time-range presets and filter chips visibly share size/shape with other shared Button instances.
  </verification>

<success_criteria>

- Four atomic commits on the current branch, each with a `style(moshpit):` or `fix(moshpit):` prefix, matching recent `moshpit-tournament` commit style.
- No `dark:` variant usage, no `:class="[]"` merges, no `!important`, no `any`/`as any` introduced.
- `pnpm typecheck` + `pnpm lint` both green after each task.
- Grep confirms old tokens removed:
  - `bg-node-component-surface` removed from MoshpitSideRail.vue and MoshpitSettingsPanel.vue.
  - `bg-interface-panel-surface` removed from MoshpitAddFilterPopover.vue and MoshpitWorkflowPicker.vue.
  - `bg-node-component-primary` removed from MoshpitGroupingToggles.vue.
  - `bg-primary-background` removed from MoshpitShowHiddenToggle.vue (focus-visible ring retained separately).
  - `border-border-subtle` removed from MoshpitAddFilterPopover.vue, MoshpitWorkflowPicker.vue, MoshpitTimeRangePicker.vue.
  - Hand-coded `h-6 rounded-full px-2 text-xs` preset-pill class gone from MoshpitTimeRangePicker.vue.
  - Hand-coded `inline-flex size-6 shrink-0 cursor-pointer items-center justify-center` chip-remove class gone from MoshpitFilterChipRow.vue.
- Small-caps section headers (`text-2xs uppercase tracking-wide`) preserved unchanged across all touched files.
  </success_criteria>

<output>
After completion, create `.planning/quick/260423-89u-align-moshpit-sidebar-and-filter-ui-to-n/260423-89u-SUMMARY.md` recording:
- Per-task diff summary (tokens changed, files touched).
- Final commit hashes for the four atomic commits.
- Confirmation that typecheck + lint passed after each task.
- Any unexpected collateral (e.g. tests that needed selector updates, hover-token collisions).
</output>

---
phase: 260423-89u-align-moshpit-sidebar
plan: 01
subsystem: moshpit/ui
tags:
  - styling
  - moshpit
  - design-alignment
dependency_graph:
  requires: []
  provides:
    - Moshpit side-panel surfaces visually match native RightSidePanel.
    - Moshpit toggle selection states share the native toggleGroup token.
    - Moshpit filter editor inputs use a single border token.
    - Moshpit filter pills and chip-remove controls route through the shared Button.
  affects: []
tech_stack:
  added: []
  patterns:
    - 'Shared Button component (variant=secondary, size=sm) for pill-style radios.'
    - 'Shared Button component (variant=textonly, size=icon-sm) for icon-only controls inside hand-coded layout wrappers.'
key_files:
  created: []
  modified:
    - src/platform/moshpit/components/MoshpitSideRail.vue
    - src/platform/moshpit/components/MoshpitSettingsPanel.vue
    - src/platform/moshpit/components/MoshpitAddFilterPopover.vue
    - src/platform/moshpit/components/MoshpitWorkflowPicker.vue
    - src/platform/moshpit/components/MoshpitGroupingToggles.vue
    - src/platform/moshpit/components/MoshpitShowHiddenToggle.vue
    - src/platform/moshpit/components/MoshpitTimeRangePicker.vue
    - src/platform/moshpit/components/MoshpitFilterChipRow.vue
decisions:
  - "Kept the outer chip wrapper (`<div class='... bg-secondary-background ...'>`) in MoshpitFilterChipRow hand-coded; it is a layout container, not a control."
  - "Preserved `size-5` on the chip remove icon (`icon-[lucide--x]`) rather than adopting Button's default icon sizing, to avoid a visible shrink."
  - "Selected-state delta on the time-range presets is applied via `cn()` override on top of Button's secondary variant rather than defining a new variant — the selected token is Moshpit-specific."
metrics:
  duration: '~12 min'
  completed: '2026-04-23'
---

# Phase 260423-89u Plan 01: Align Moshpit Sidebar and Filter UI to Native ComfyUI Summary

Aligned four styling gaps between the Moshpit side-panel UI and native ComfyUI: panel surfaces on `bg-comfy-menu-bg`, selection states on `bg-interface-menu-component-surface-selected`, filter-editor/input borders on `border-border-default`, and hand-coded pills/chips routed through the shared `Button` component.

## Per-Task Diff Summary

### Task 1 — Align panel surfaces to bg-comfy-menu-bg (commit `0cb7af029`)

| File                        | Token change                                                        |
| --------------------------- | ------------------------------------------------------------------- |
| MoshpitSideRail.vue         | `bg-node-component-surface` → `bg-comfy-menu-bg`                    |
| MoshpitSettingsPanel.vue    | `bg-node-component-surface` → `bg-comfy-menu-bg`                    |
| MoshpitAddFilterPopover.vue | `bg-interface-panel-surface` → `bg-comfy-menu-bg` (PopoverContent)  |
| MoshpitWorkflowPicker.vue   | `bg-interface-panel-surface` → `bg-comfy-menu-bg` (ComboboxContent) |

Typecheck green. Lint (touched files) green. Commit message: `style(moshpit): align panel surfaces to bg-comfy-menu-bg`.

### Task 2 — Align selection states to native toggle token (commit `57ad0c2bc`)

| File                        | Token change                                                                              |
| --------------------------- | ----------------------------------------------------------------------------------------- |
| MoshpitGroupingToggles.vue  | Active pill: `bg-node-component-primary` → `bg-interface-menu-component-surface-selected` |
| MoshpitShowHiddenToggle.vue | On-state: `bg-primary-background` → `bg-interface-menu-component-surface-selected`        |

Off-state background, focus-visible ring, and switch thumb all left untouched. Typecheck + lint green. Commit message: `style(moshpit): align selection states to native toggle token`.

### Task 3 — Consolidate filter input borders on border-border-default (commit `c91113cac`)

| File                        | Locations                                                            |
| --------------------------- | -------------------------------------------------------------------- |
| MoshpitAddFilterPopover.vue | PopoverContent, Step-1 search input, `triggerClass` (block + inline) |
| MoshpitWorkflowPicker.vue   | Anchor button, ComboboxContent, ComboboxInput                        |
| MoshpitTimeRangePicker.vue  | Both `<input type="date">` elements (From/To)                        |

All `border-border-subtle` tokens in the three files replaced with `border-border-default`. `border-(--interface-stroke)` on outer panel shells intentionally left unchanged. Typecheck + lint green. Commit message: `style(moshpit): consolidate filter input borders on border-border-default`.

### Task 4 — Replace hand-coded pills with shared Button (commit `c6b2c211a`)

| File                       | Change                                                                                                                    |
| -------------------------- | ------------------------------------------------------------------------------------------------------------------------- |
| MoshpitTimeRangePicker.vue | Preset `<button>` → `<Button size="sm" variant="secondary">`. Selected-state overlay via `cn()`. Import added.            |
| MoshpitFilterChipRow.vue   | Chip remove `<button>` → `<Button size="icon-sm" variant="textonly">`. Preserved `icon-[lucide--x] size-5`. Import added. |

Aria-label, role, aria-checked, key, and click handlers preserved verbatim. 23/23 unit tests green across `MoshpitFilterChipRow.test.ts` + `MoshpitTimeRangePicker.test.ts`. Format + typecheck + lint all green. Commit message: `fix(moshpit): replace hand-coded pills with shared Button`.

## Commits

| Task | Commit      | Message                                                                     |
| ---- | ----------- | --------------------------------------------------------------------------- |
| 1    | `0cb7af029` | `style(moshpit): align panel surfaces to bg-comfy-menu-bg`                  |
| 2    | `57ad0c2bc` | `style(moshpit): align selection states to native toggle token`             |
| 3    | `c91113cac` | `style(moshpit): consolidate filter input borders on border-border-default` |
| 4    | `c6b2c211a` | `fix(moshpit): replace hand-coded pills with shared Button`                 |

## Verification

- `pnpm typecheck` green after every task.
- `pnpm lint` run on touched files after every task; the 5 project-wide lint errors that surfaced (thumbWorker floating promise + unrelated `no-floating-promises` / `consistent-type-imports` violations in `src/renderer/**`, `src/platform/cloud/**`, `src/workbench/**`, etc.) are pre-existing on `moshpit` and out of scope.
- `pnpm format` run after Task 4 per the constraints; no regressions introduced by oxfmt in the two touched files.
- Focused unit tests: `MoshpitFilterChipRow.test.ts` + `MoshpitTimeRangePicker.test.ts` → 23/23 green after the Button substitution. Tests rely on role/aria-label selectors which remain stable (Button's `Primitive` still renders a native `<button>`).

## Unexpected Collateral

**oxfmt side-commit `097cabe75` (`chore: apply oxfmt sweep + fix stale import-style lint errors`) landed mid-plan.** When `pnpm format` ran during Task 4, oxfmt reformatted across the full 3123-file tree and picked up a significant amount of pre-existing dirty working-tree content (summary frontmatter YAML style, JSON sort, a worker import fix) that the project had accumulated. The commit is narrow and mechanical (idempotent oxfmt + three typed-import fixes in pre-existing files) and predates the four planned commits. No behavioural change. Flagging here so the orchestrator is aware the branch contains five net commits from this plan, not four.

Root cause: the constraint "run `pnpm format` after task 4" is a project-wide formatter; a Moshpit-scoped execution would be `pnpm exec oxfmt --write src/platform/moshpit/components/*.vue`. For future plans, narrow the format step to the touched files if working-tree drift is a concern.

## Deviations from Plan

None — plan executed exactly as written. No Rule-1..3 auto-fixes required; no Rule-4 architectural decisions surfaced. Selected-state override on time-range presets implemented per the plan's example shape. Chip remove control sized per the plan note on `size-5` icon preservation.

## Threat Flags

None — pure styling changes, no new trust boundaries, no schema/auth surface touched.

## Self-Check: PASSED

**Files touched (all present on disk):**

- `src/platform/moshpit/components/MoshpitSideRail.vue` — FOUND
- `src/platform/moshpit/components/MoshpitSettingsPanel.vue` — FOUND
- `src/platform/moshpit/components/MoshpitAddFilterPopover.vue` — FOUND
- `src/platform/moshpit/components/MoshpitWorkflowPicker.vue` — FOUND
- `src/platform/moshpit/components/MoshpitGroupingToggles.vue` — FOUND
- `src/platform/moshpit/components/MoshpitShowHiddenToggle.vue` — FOUND
- `src/platform/moshpit/components/MoshpitTimeRangePicker.vue` — FOUND
- `src/platform/moshpit/components/MoshpitFilterChipRow.vue` — FOUND

**Commits (verified in `git log --oneline -5`):**

- `c6b2c211a` — FOUND
- `c91113cac` — FOUND
- `57ad0c2bc` — FOUND
- `0cb7af029` — FOUND

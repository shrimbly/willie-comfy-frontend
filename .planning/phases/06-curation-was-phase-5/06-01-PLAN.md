---
phase: 06-curation-was-phase-5
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/moshpit/services/thumbRepository.types.ts
  - src/platform/moshpit/services/thumbRepository.ts
  - src/platform/moshpit/services/thumbRepository.test.ts
  - src/platform/moshpit/services/curationRepository.ts
  - src/platform/moshpit/services/curationRepository.test.ts
  - src/platform/moshpit/services/foldersRepository.ts
  - src/platform/moshpit/services/foldersRepository.test.ts
  - src/platform/moshpit/stores/moshpitCurationStore.ts
  - src/platform/moshpit/stores/moshpitCurationStore.test.ts
  - src/platform/moshpit/stores/moshpitFoldersStore.ts
  - src/platform/moshpit/stores/moshpitFoldersStore.test.ts
autonomous: true
requirements:
  - CURATE-01
  - CURATE-02
  - CURATE-03
  - CURATE-04

must_haves:
  truths:
    - 'MoshpitDB opens at version 5 with a `folders` object store keyed by `id`'
    - 'curationRepository.saveCuration(hash, curation) round-trips via fake-indexeddb and preserves AssetMetaRecord invariants'
    - 'curationRepository.saveManyCurations(map) writes all records in a single transaction (bulk flush)'
    - 'foldersRepository CRUD (getAll / save / delete / clear) round-trips via fake-indexeddb'
    - 'moshpitCurationStore exposes setFavourite, addTag, removeTag, setHidden, addToFolder, removeFromFolder, applyManyOptimistic with debounced per-hash persist (single-asset) and immediate bulk flush (many-asset)'
    - 'moshpitFoldersStore exposes folders, orderedFolders, hydrate, create, rename, remove, createFromSelection and scrubs deleted folder ids from CurationRecord.folders[] on remove'
    - 'Every mutation wraps a new Map assignment so Pinia watchers re-fire'
    - 'Persistence failures console.warn and never throw (mirror moshpitOverrideStore.logPersistError)'
  artifacts:
    - path: 'src/platform/moshpit/services/thumbRepository.types.ts'
      provides: 'FolderRecord interface + MOSHPIT_DB_VERSION = 5 + MoshpitDB.folders store'
      contains: 'interface FolderRecord'
    - path: 'src/platform/moshpit/services/thumbRepository.ts'
      provides: 'v4→v5 upgrade hook that creates the folders object store'
      contains: 'if (oldVersion < 5)'
    - path: 'src/platform/moshpit/services/curationRepository.ts'
      provides: 'saveCuration + saveManyCurations (single assetMeta transaction) + loadAllCurations'
      exports: ['saveCuration', 'saveManyCurations', 'loadAllCurations']
    - path: 'src/platform/moshpit/services/foldersRepository.ts'
      provides: 'loadAllFolders + saveFolder + deleteFolder + clearAllFolders'
      exports:
        ['loadAllFolders', 'saveFolder', 'deleteFolder', 'clearAllFolders']
    - path: 'src/platform/moshpit/stores/moshpitCurationStore.ts'
      provides: 'Mutation actions + debounced persist + bulk flush'
      contains: 'setFavourite'
    - path: 'src/platform/moshpit/stores/moshpitFoldersStore.ts'
      provides: 'Folders Pinia store with createFromSelection + referential-integrity scrub on remove'
      exports: ['useMoshpitFoldersStore']
  key_links:
    - from: 'curationRepository.saveCuration'
      to: "thumbRepository.getMoshpitDB + 'assetMeta' store"
      via: 'read-modify-write of AssetMetaRecord.curation (ASSET-04 invariant: curation nested in assetMeta)'
      pattern: "getMoshpitDB().*'assetMeta'"
    - from: 'moshpitCurationStore.setFavourite/addTag/setHidden/addToFolder'
      to: 'curationRepository.saveCuration'
      via: 'schedulePersist 100ms debounce per hash'
      pattern: 'schedulePersist'
    - from: 'moshpitCurationStore.applyManyOptimistic'
      to: 'curationRepository.saveManyCurations'
      via: 'single transaction, no debounce (Pitfall 2 — avoid IDB write amplification on bulk)'
      pattern: 'saveManyCurations'
    - from: 'moshpitFoldersStore.remove'
      to: 'moshpitCurationStore.removeFromFolder'
      via: 'iterate curationByHash, strip deleted folder id from each CurationRecord.folders[] before deleting FolderRecord (Pitfall 4 — dangling references)'
      pattern: 'removeFromFolder'
---

<objective>
Build the data layer foundation for Phase 6 curation: IndexedDB v5 migration, a `curationRepository` mirroring the shape of `overrideRepository`, a `foldersRepository`, and the mutation actions on `moshpitCurationStore` + a new `moshpitFoldersStore`. No UI, no toast, no keybindings yet — pure data + stores.

Purpose: downstream plans (02 filters, 03 orchestrator, 04/05 UI) depend on these stores being mutable and persisted. Isolating the data layer here keeps wave 2/3 plans free to work in parallel.

Output: MoshpitDB at v5 with `folders` store; `curationRepository.ts` + `foldersRepository.ts`; extended `moshpitCurationStore` with mutation actions; new `moshpitFoldersStore`; full test coverage (unit + fake-indexeddb round-trip + DB upgrade assertion).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-curation-was-phase-5/06-RESEARCH.md
@CLAUDE.md
@AGENTS.md

<interfaces>
<!-- Key types the executor must implement against. Extracted from shipped code; do not re-explore. -->

From src/platform/moshpit/services/thumbRepository.types.ts (existing):

```typescript
export interface CurationRecord {
  readonly favourite: boolean
  readonly tags: readonly string[]
  readonly folders: readonly string[] // ← holds folder ids (not names)
  readonly hidden: boolean
}

export interface AssetMetaRecord {
  readonly contentHash: string
  readonly metadata: Readonly<Record<string, string>>
  readonly curation: CurationRecord // ← curation is nested, not separate store (ASSET-04)
  readonly params: NormalizedParams
}

export interface MoshpitDB extends DBSchema {
  thumbs: { key: string; value: ThumbRecord }
  assetMeta: { key: string; value: AssetMetaRecord }
  overrides: { key: string; value: OverridePersistedRecord }
}

export const MOSHPIT_DB_NAME = 'moshpit-v1'
export const MOSHPIT_DB_VERSION = 4 // ← BUMP TO 5 in this plan
```

From src/platform/moshpit/services/thumbRepository.ts (existing helpers):

```typescript
export function getMoshpitDB(): Promise<IDBPDatabase<MoshpitDB>>
export function defaultCuration(): CurationRecord
export async function putAssetMeta(record: AssetMetaRecord): Promise<void>
export async function getAssetMeta(
  contentHash: string
): Promise<AssetMetaRecord | undefined>
```

From src/platform/moshpit/services/overrideRepository.ts (PATTERN TO MIRROR — do not modify):

```typescript
// Uses getMoshpitDB() — single DB handle shared across repositories.
// Validates records with isValidRecord type guard on read.
// Swallows no errors — callers decide; store.ts wraps every call in .catch(logPersistError).
export async function loadAllOverrides(): Promise<OverridePersistedRecord[]>
export async function saveOverride(
  record: OverridePersistedRecord
): Promise<void>
export async function deleteOverride(contentHash: string): Promise<void>
export async function clearAllOverrides(): Promise<void>
```

From src/platform/moshpit/stores/moshpitOverrideStore.ts (PATTERN TO MIRROR for debounced persist):

```typescript
const PERSIST_DEBOUNCE_MS = 100
const pendingTimers = new Map<string, ReturnType<typeof setTimeout>>()
function schedulePersist(hash: string): void {
  cancelPending(hash)
  const timer = setTimeout(() => {
    pendingTimers.delete(hash)
    // read current in-memory state, write to repo, .catch(logPersistError)
  }, PERSIST_DEBOUNCE_MS)
  pendingTimers.set(hash, timer)
}
```

From src/platform/moshpit/stores/moshpitCurationStore.ts (EXTEND — current scaffold):

```typescript
export const useMoshpitCurationStore = defineStore('moshpitCuration', () => {
  const curationByHash = ref(new Map<string, CurationRecord>())
  function load(record: AssetMetaRecord): void
  function get(contentHash: string): CurationRecord | undefined
  function reset(): void
  return { load, get, reset }
})
```

</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend MoshpitDB to v5 with folders store + curationRepository + foldersRepository</name>
  <files>
    src/platform/moshpit/services/thumbRepository.types.ts,
    src/platform/moshpit/services/thumbRepository.ts,
    src/platform/moshpit/services/thumbRepository.test.ts,
    src/platform/moshpit/services/curationRepository.ts,
    src/platform/moshpit/services/curationRepository.test.ts,
    src/platform/moshpit/services/foldersRepository.ts,
    src/platform/moshpit/services/foldersRepository.test.ts
  </files>
  <read_first>
    - src/platform/moshpit/services/thumbRepository.ts (full file — v1→v4 migration pattern is the template)
    - src/platform/moshpit/services/thumbRepository.types.ts (CurationRecord + MoshpitDB + MOSHPIT_DB_VERSION)
    - src/platform/moshpit/services/overrideRepository.ts (full file — repository shape to mirror)
    - src/platform/moshpit/services/overrideRepository.test.ts (test shape to mirror)
    - src/platform/moshpit/services/thumbRepository.test.ts (extend with v4→v5 migration test)
  </read_first>
  <behavior>
    - Test: openMoshpitDB on a fresh DB succeeds at version 5 and contains `thumbs`, `assetMeta`, `overrides`, `folders` object stores.
    - Test: v4→v5 upgrade path (seed DB at v4, re-open at v5) adds `folders` store without throwing and preserves existing `assetMeta` / `overrides` records.
    - Test: curationRepository.saveCuration(hash, next) reads the existing AssetMetaRecord, merges `curation: next`, and writes it back; hash without assetMeta is a no-op (no-throw, no insert).
    - Test: curationRepository.saveManyCurations(Map<hash, CurationRecord>) writes all records in a single readwrite transaction on `assetMeta`; hashes without assetMeta are skipped.
    - Test: curationRepository.loadAllCurations returns Map<hash, CurationRecord> from every assetMeta record.
    - Test: foldersRepository.loadAllFolders + saveFolder + deleteFolder + clearAllFolders round-trip a FolderRecord via fake-indexeddb.
    - Test: foldersRepository.loadAllFolders skips malformed records (mirror overrideRepository.isValidRecord guard) and console.warns.
  </behavior>
  <action>
    Step 1 — thumbRepository.types.ts:
    - Add `export interface FolderRecord { readonly id: string; readonly name: string; readonly createdAt: number }` above MoshpitDB.
    - Extend `MoshpitDB` with `folders: { key: string; value: FolderRecord }`.
    - Bump `export const MOSHPIT_DB_VERSION = 5`.
    - Add a comment block documenting v5: "v5 (Phase 6 Plan 01): `folders` object store added. Keyed by folder id (uuid). CurationRecord.folders[] holds folder ids that reference this store. No per-record migration — pre-v5 data has folders: [] universally."

    Step 2 — thumbRepository.ts upgrade hook (append inside the `async upgrade(...)` callback, AFTER the existing `if (oldVersion < 4)` block):
    ```ts
    if (oldVersion < 5) {
      if (!db.objectStoreNames.contains('folders')) {
        db.createObjectStore('folders', { keyPath: 'id' })
      }
      // No per-record migration needed: pre-v5 CurationRecord.folders[] is
      // universally empty (no Phase 6 UI shipped). Dangling folder ids
      // (should they exist) become inert references on read — harmless.
    }
    ```
    Do NOT touch earlier branches. Do NOT change `deleteMoshpitDB` or `openMoshpitDB` cache semantics.

    Step 3 — curationRepository.ts:
    - Import `getMoshpitDB` from `./thumbRepository`.
    - Import `CurationRecord, AssetMetaRecord` from `./thumbRepository.types`.
    - Export `async function saveCuration(hash: string, curation: CurationRecord): Promise<void>`:
      - `const db = await getMoshpitDB()`
      - `const existing = await db.get('assetMeta', hash)`
      - `if (!existing) return`
      - `await db.put('assetMeta', { ...existing, curation })`
    - Export `async function saveManyCurations(updates: ReadonlyMap<string, CurationRecord>): Promise<void>`:
      - Open a `readwrite` transaction on `'assetMeta'`
      - For each [hash, curation] in updates: `const existing = await tx.store.get(hash); if (existing) await tx.store.put({ ...existing, curation })`
      - `await tx.done`
    - Export `async function loadAllCurations(): Promise<Map<string, CurationRecord>>`:
      - Open `assetMeta` store, iterate all records, build `Map<hash, record.curation>`, return.

    Step 4 — foldersRepository.ts (mirror overrideRepository.ts structure):
    - Import `getMoshpitDB` from `./thumbRepository`.
    - Import `FolderRecord` from `./thumbRepository.types`.
    - Add a private `isValidFolderRecord(record: unknown): record is FolderRecord` type guard that checks `id: non-empty string`, `name: string` (may be empty after trim but accept), `createdAt: finite number`.
    - Export `loadAllFolders(): Promise<FolderRecord[]>` — getAll('folders'), filter invalid with `console.warn`.
    - Export `saveFolder(record: FolderRecord): Promise<void>` — db.put('folders', record).
    - Export `deleteFolder(id: string): Promise<void>` — db.delete('folders', id).
    - Export `clearAllFolders(): Promise<void>` — db.clear('folders').

    Step 5 — Tests:
    - curationRepository.test.ts: import `fake-indexeddb/auto`, `deleteMoshpitDB` before each test, seed one `assetMeta` record via `putAssetMeta`, assert saveCuration round-trips; assert save on missing-hash is silent no-op; assert saveManyCurations writes all three of [present, missing, present] records.
    - foldersRepository.test.ts: round-trip a FolderRecord; deleteFolder removes it; clearAllFolders empties the store; loadAllFolders skips a record written with `{ id: 42 }` (invalid).
    - thumbRepository.test.ts: add a new `describe('v4→v5 migration')` block. Manually open DB at version 4 via raw `indexedDB.open('moshpit-v1', 4)` (OR call `openMoshpitDB` once, close, then bump to 5 via another call — use deleteMoshpitDB between) and assert after upgrade `objectStoreNames.contains('folders') === true` and `objectStoreNames.contains('assetMeta') === true`.

    Do NOT use `any`, `as any`, or `@ts-expect-error`. If narrowing from `unknown` in isValidFolderRecord, use plain typeof checks.

  </action>
  <acceptance_criteria>
    - grep: `MOSHPIT_DB_VERSION = 5` in src/platform/moshpit/services/thumbRepository.types.ts
    - grep: `interface FolderRecord` in src/platform/moshpit/services/thumbRepository.types.ts
    - grep: `if (oldVersion < 5)` in src/platform/moshpit/services/thumbRepository.ts
    - grep: `createObjectStore('folders'` in src/platform/moshpit/services/thumbRepository.ts
    - file exists: src/platform/moshpit/services/curationRepository.ts
    - grep: `export async function saveCuration` and `export async function saveManyCurations` and `export async function loadAllCurations` in src/platform/moshpit/services/curationRepository.ts
    - grep: `readwrite` and `tx.store.put` in src/platform/moshpit/services/curationRepository.ts (bulk transaction)
    - file exists: src/platform/moshpit/services/foldersRepository.ts
    - grep: `export async function loadAllFolders` and `export async function saveFolder` and `export async function deleteFolder` and `export async function clearAllFolders` in src/platform/moshpit/services/foldersRepository.ts
    - grep: `v4→v5 migration` or `v4-v5 migration` or `v5 migration` in src/platform/moshpit/services/thumbRepository.test.ts
    - file exists: src/platform/moshpit/services/curationRepository.test.ts
    - file exists: src/platform/moshpit/services/foldersRepository.test.ts
    - No occurrences of `any`, `as any`, or `@ts-expect-error` in the new files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/platform/moshpit/services/thumbRepository.test.ts src/platform/moshpit/services/curationRepository.test.ts src/platform/moshpit/services/foldersRepository.test.ts</automated>
  </verify>
  <done>All three test files green under fake-indexeddb; `pnpm typecheck` passes; MoshpitDB opens at v5 with a `folders` store on both fresh DBs and upgraded v4 DBs.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Extend moshpitCurationStore with mutation actions + create moshpitFoldersStore</name>
  <files>
    src/platform/moshpit/stores/moshpitCurationStore.ts,
    src/platform/moshpit/stores/moshpitCurationStore.test.ts,
    src/platform/moshpit/stores/moshpitFoldersStore.ts,
    src/platform/moshpit/stores/moshpitFoldersStore.test.ts
  </files>
  <read_first>
    - src/platform/moshpit/stores/moshpitCurationStore.ts (existing scaffold — extend, do not rewrite)
    - src/platform/moshpit/stores/moshpitCurationStore.test.ts (extend, do not rewrite)
    - src/platform/moshpit/stores/moshpitOverrideStore.ts (debounced-persist pattern + schedulePersist + logPersistError to mirror)
    - src/platform/moshpit/services/thumbRepository.ts (defaultCuration helper — reuse, do not re-implement)
    - src/platform/moshpit/services/curationRepository.ts (from Task 1 — the persist target)
    - src/platform/moshpit/services/foldersRepository.ts (from Task 1 — the persist target)
  </read_first>
  <behavior>
    - Test: setFavourite(hash, true) on a fresh store creates CurationRecord { favourite:true, tags:[], folders:[], hidden:false } in curationByHash and schedules a persist.
    - Test: setFavourite(hash, true) followed by setFavourite(hash, true) is idempotent (no second persist scheduled after the first timer fires — optional strict check).
    - Test: addTag(hash, 'hero') then addTag(hash, 'hero') results in exactly one 'hero' in tags (dedupe at mutation).
    - Test: removeTag(hash, 'hero') on a non-existent hash is a no-op (no throw, no record created).
    - Test: setHidden(hash, true) updates `hidden: true` and schedules persist.
    - Test: addToFolder(hash, 'folder-uuid') appends the id; removeFromFolder removes it.
    - Test: applyManyOptimistic(Map<hash, CurationRecord>) replaces in-memory entries in one Map swap (single reactivity tick) and awaits saveManyCurations.
    - Test (foldersStore): create('Shortlist A') returns a uuid, folders Map gains the entry, saveFolder was called.
    - Test (foldersStore): createFromSelection('Winners', ['h1', 'h2', 'h3']) creates the folder AND calls curationStore.addToFolder for each hash.
    - Test (foldersStore): remove(id) scrubs `id` from every CurationRecord.folders[] in curationStore.curationByHash BEFORE deleting the FolderRecord (Pitfall 4). Simulate by seeding two hashes with folders: ['x'] and asserting after remove('x') both have folders: [].
    - Test (foldersStore): rename(id, 'New name') updates the FolderRecord.name and schedules persist.
    - Test (foldersStore): hydrate() loads all folders from foldersRepository.loadAllFolders into the Map.
  </behavior>
  <action>
    Step 1 — moshpitCurationStore.ts extension (keep existing load/get/reset):
    - Import `saveCuration, saveManyCurations` from `../services/curationRepository`.
    - Import `defaultCuration` from `../services/thumbRepository`.
    - Add `const PERSIST_DEBOUNCE_MS = 100` and a `pendingTimers = new Map<string, ReturnType<typeof setTimeout>>()` inside the store setup.
    - Add `function logPersistError(err: unknown): void { console.warn('[moshpit] curation persist failed', err) }`.
    - Add `function schedulePersist(hash: string): void` mirroring moshpitOverrideStore.schedulePersist but calling `saveCuration(hash, current).catch(logPersistError)` (reads current from curationByHash.value.get(hash), defaults via defaultCuration() if undefined).
    - Add a private `applyUpdate(hash, updater: (cur: CurationRecord) => CurationRecord): void`:
      - `const current = curationByHash.value.get(hash) ?? defaultCuration()`
      - `const next = updater(current)`
      - Shallow-equal guard — if next === current or JSON-equal skip; otherwise:
      - `const map = new Map(curationByHash.value); map.set(hash, next); curationByHash.value = map; schedulePersist(hash)`
    - Add exports:
      - `setFavourite(hash, favourite)` → applyUpdate with `{ ...cur, favourite }`
      - `addTag(hash, tag)` → applyUpdate with tags = [...new Set([...cur.tags, tag])]
      - `removeTag(hash, tag)` → applyUpdate with tags = cur.tags.filter((t) => t !== tag)
      - `setHidden(hash, hidden)` → applyUpdate with `{ ...cur, hidden }`
      - `addToFolder(hash, folderId)` → applyUpdate with folders = [...new Set([...cur.folders, folderId])]
      - `removeFromFolder(hash, folderId)` → applyUpdate with folders = cur.folders.filter((f) => f !== folderId)
    - Add `async function applyManyOptimistic(updates: ReadonlyMap<string, CurationRecord>): Promise<void>`:
      - Cancel any pending per-hash timers for hashes in `updates` (avoid double-write).
      - Build `next = new Map(curationByHash.value)`; for each [hash, rec] `next.set(hash, rec)`.
      - `curationByHash.value = next`
      - `await saveManyCurations(updates).catch(logPersistError)`
    - Add `hydrate()` — async function that calls `loadAllCurations` from curationRepository and populates curationByHash (uses Map swap). Optional but recommended for symmetry with overrideStore.
    - Return all new exports alongside existing load/get/reset.

    Step 2 — moshpitFoldersStore.ts (new file):
    - `import { defineStore } from 'pinia'; import { computed, ref } from 'vue'`
    - Import `FolderRecord` from `../services/thumbRepository.types`.
    - Import `loadAllFolders, saveFolder, deleteFolder, clearAllFolders` from `../services/foldersRepository`.
    - Import `useMoshpitCurationStore` from `./moshpitCurationStore`.
    - Mirror the debounce pattern from overrideStore: PERSIST_DEBOUNCE_MS = 100, pendingTimers per-id.
    - `folders = ref(new Map<string, FolderRecord>())`
    - `orderedFolders = computed(() => [...folders.value.values()].sort((a, b) => a.createdAt - b.createdAt))`
    - `async function hydrate(): Promise<void>` — loads from foldersRepository, handles error with console.warn.
    - `function create(name: string): string` — generate uuid via `crypto.randomUUID()`, create FolderRecord, update Map (swap new Map), schedulePersist, return id.
    - `function rename(id: string, name: string): void` — update Map, schedulePersist.
    - `function remove(id: string): void` — see next step:
      1. Get curationStore via `useMoshpitCurationStore()` — safe inside a store action.
      2. Iterate `curationStore.curationByHash`; for every hash whose `folders.includes(id)`, call `curationStore.removeFromFolder(hash, id)` (Pitfall 4 scrub).
      3. Cancel pending timer for id, delete from Map (Map swap), `deleteFolder(id).catch(logPersistError)`.
    - `function createFromSelection(name: string, hashes: readonly string[]): string`:
      1. `const id = create(name)`
      2. For each hash: `curationStore.addToFolder(hash, id)`
      3. Return id.
    - Return `{ folders, orderedFolders, hydrate, create, rename, remove, createFromSelection }`.

    Step 3 — Tests:
    - moshpitCurationStore.test.ts: add a new `describe('mutations')` block. Use `createPinia() + setActivePinia()` per test. Use `fake-indexeddb/auto` and seed an assetMeta record via putAssetMeta before each mutation-persist test. Use `vi.useFakeTimers()` + `vi.runAllTimersAsync()` to flush the 100ms debounce. Assert curationByHash entries AND (after timers flush) that `getAssetMeta(hash)` returns the expected CurationRecord.
    - moshpitCurationStore.test.ts: add a test `applyManyOptimistic writes a single transaction` — spy on saveManyCurations via `vi.mock` of the repository module (per project convention; see filterMath/overrideRepository test patterns).
    - moshpitFoldersStore.test.ts: new file. createPinia, mount both stores. Cover all cases from `<behavior>`, especially the scrub-on-remove (Pitfall 4).

    Do NOT import `any`. Do NOT use `as any`. Use `vi.mocked(...)` or `vi.spyOn` for repository spies.

  </action>
  <acceptance_criteria>
    - grep: `function setFavourite` and `function addTag` and `function removeTag` and `function setHidden` and `function addToFolder` and `function removeFromFolder` and `function applyManyOptimistic` in src/platform/moshpit/stores/moshpitCurationStore.ts
    - grep: `PERSIST_DEBOUNCE_MS = 100` in src/platform/moshpit/stores/moshpitCurationStore.ts
    - grep: `schedulePersist` in src/platform/moshpit/stores/moshpitCurationStore.ts
    - grep: `logPersistError` in src/platform/moshpit/stores/moshpitCurationStore.ts
    - grep: `new Map(curationByHash.value)` in src/platform/moshpit/stores/moshpitCurationStore.ts (Map swap pattern for Pinia reactivity)
    - file exists: src/platform/moshpit/stores/moshpitFoldersStore.ts
    - grep: `defineStore('moshpitFolders'` in src/platform/moshpit/stores/moshpitFoldersStore.ts
    - grep: `crypto.randomUUID` in src/platform/moshpit/stores/moshpitFoldersStore.ts
    - grep: `removeFromFolder` in src/platform/moshpit/stores/moshpitFoldersStore.ts (referential-integrity scrub)
    - grep: `createFromSelection` in src/platform/moshpit/stores/moshpitFoldersStore.ts
    - file exists: src/platform/moshpit/stores/moshpitFoldersStore.test.ts
    - grep: `describe('mutations'` OR equivalent in src/platform/moshpit/stores/moshpitCurationStore.test.ts
    - No `any` or `as any` in the new/extended files.
  </acceptance_criteria>
  <verify>
    <automated>pnpm test:unit -- src/platform/moshpit/stores/moshpitCurationStore.test.ts src/platform/moshpit/stores/moshpitFoldersStore.test.ts</automated>
  </verify>
  <done>Both store test files green; debounced persist confirmed via fake-timers; foldersStore.remove scrubs dangling refs before deleting; `pnpm typecheck` passes.</done>
</task>

</tasks>

<threat_model>

## Trust Boundaries

| Boundary           | Description                                                                                                                                          |
| ------------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------- |
| UI → store → IDB   | User input (tag names, folder names) enters the store and persists to IndexedDB. No sanitisation needed for storage — Vue `{{ }}` escapes on render. |
| Store ↔ repository | Pinia store calls repository functions; repository reads/writes IDB. No auth gate (client-only).                                                     |
| v4 → v5 DB upgrade | Migration runs once per browser; must be idempotent and non-destructive.                                                                             |

## STRIDE Threat Register

| Threat ID  | Category                | Component                                          | Disposition | Mitigation Plan                                                                                                                                                                                                      |
| ---------- | ----------------------- | -------------------------------------------------- | ----------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| T-06-01-01 | Tampering               | v4→v5 DB upgrade                                   | mitigate    | Use `if (!db.objectStoreNames.contains('folders'))` guard; never throw from upgrade; no per-record migration so no partial-failure mode.                                                                             |
| T-06-01-02 | Denial of Service       | unbounded folder count                             | mitigate    | Cap folder count at 200 in `moshpitFoldersStore.create` — reject with no-op if `folders.value.size >= 200`. Toast/i18n happens in Plan 03 orchestrator; store returns empty string on cap-hit so callers can detect. |
| T-06-01-03 | Denial of Service       | unbounded tag length / count per asset             | mitigate    | Cap tag length at 64 chars and tag count at 50 per asset in `addTag` — trim, reject (no-op) if post-trim empty, length > 64, or `cur.tags.length >= 50`.                                                             |
| T-06-01-04 | Information Disclosure  | foldersRepository.loadAllFolders reads raw records | mitigate    | `isValidFolderRecord` type guard rejects malformed records with `console.warn` (mirror overrideRepository pattern).                                                                                                  |
| T-06-01-05 | Repudiation / data loss | IDB write failure during bulk flush                | accept      | `applyManyOptimistic.catch(logPersistError)` — failures are logged, in-memory state preserved, next single-asset mutation will re-persist. Acceptable for v1 client-only tool.                                       |
| T-06-01-06 | Tampering               | foldersStore.remove leaving dangling ids           | mitigate    | Scrub dangling folder ids from every CurationRecord.folders[] BEFORE deleting the FolderRecord (Pitfall 4). Test coverage required.                                                                                  |

</threat_model>

<verification>
- `pnpm test:unit -- src/platform/moshpit/services/thumbRepository.test.ts src/platform/moshpit/services/curationRepository.test.ts src/platform/moshpit/services/foldersRepository.test.ts src/platform/moshpit/stores/moshpitCurationStore.test.ts src/platform/moshpit/stores/moshpitFoldersStore.test.ts` — all green
- `pnpm typecheck` — green
- `pnpm lint` — no new violations in touched files
- Manual: open browser DevTools → Application → IndexedDB → moshpit-v1 on a new session and confirm version is 5 with four object stores.
</verification>

<success_criteria>

- MoshpitDB v5 ships with `folders` object store; v4→v5 upgrade is non-destructive.
- curationRepository + foldersRepository mirror the overrideRepository shape (shared DB handle, swallow-on-warn semantics at the store layer).
- moshpitCurationStore gains six single-hash mutation actions (+ applyManyOptimistic) with debounced persistence; bulk flush uses a single IDB transaction (Pitfall 2).
- moshpitFoldersStore exists with ordered folders, create/rename/remove/createFromSelection; remove() scrubs dangling ids before deletion (Pitfall 4).
- Tag length cap (64) + tag count cap (50) + folder count cap (200) enforced at mutation time (T-06-01-02/03).
- All new files free of `any`, `as any`, `@ts-expect-error`.
- 625+ existing moshpit unit tests remain green; new tests for this plan added.
  </success_criteria>

<output>
After completion, create `.planning/phases/06-curation-was-phase-5/06-01-SUMMARY.md` noting:
- Final MOSHPIT_DB_VERSION value and any migration nuance observed.
- Caps enforced (tag length 64, tag count 50/asset, folder count 200).
- Any deviation from research (e.g., if applyManyOptimistic semantics differ).
- Test file list and count.
- Pointers for Plan 02 (filter integration), Plan 03 (orchestrator consumes these stores), Plan 05 (UI consumes foldersStore).
</output>

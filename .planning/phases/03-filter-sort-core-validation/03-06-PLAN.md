---
phase: 03-filter-sort-core-validation
plan: 06
type: execute
wave: 2
depends_on: [02]
files_modified:
  - src/locales/en/main.json
  - src/platform/moshpit/components/MoshpitWorkflowPicker.vue
  - src/platform/moshpit/components/MoshpitWorkflowPicker.test.ts
  - src/platform/moshpit/components/MoshpitTimeRangePicker.vue
  - src/platform/moshpit/components/MoshpitTimeRangePicker.test.ts
  - src/platform/moshpit/composables/useMoshpitWorkflowOptions.ts
  - src/platform/moshpit/composables/useMoshpitWorkflowOptions.test.ts
autonomous: true
requirements: [FILTER-01, FILTER-06]
tags: [moshpit, filter-gate, ui, i18n, reka-ui]

must_haves:
  truths:
    - "All Phase 3 i18n keys exist in src/locales/en/main.json under moshpit.filters.* and moshpit.sort.*"
    - "MoshpitWorkflowPicker renders as Reka ComboboxRoot of workflow options with asset counts"
    - "Workflow options come from grouping registry assets by params.workflowFingerprint"
    - "Selecting a workflow calls filterStore.setWorkflow(fingerprint)"
    - "MoshpitTimeRangePicker renders 5 preset pills + From/To date inputs visible only when custom active"
    - "Selecting a preset calls filterStore.setTimeRange with correct { preset, from: null, to: null } payload"
    - "Neither component uses PrimeVue; native HTML date inputs for custom range"
  artifacts:
    - path: "src/platform/moshpit/components/MoshpitWorkflowPicker.vue"
      provides: "Reka single-select combobox wired to moshpitFilterStore.setWorkflow"
    - path: "src/platform/moshpit/components/MoshpitTimeRangePicker.vue"
      provides: "Preset pills + custom date range wired to moshpitFilterStore.setTimeRange"
    - path: "src/platform/moshpit/composables/useMoshpitWorkflowOptions.ts"
      provides: "Computed { fingerprint, displayName, count } options grouped by workflowFingerprint"
      exports: ["useMoshpitWorkflowOptions", "WorkflowOption"]
    - path: "src/locales/en/main.json"
      provides: "moshpit.filters.* (38 keys) + moshpit.sort.* (12 keys) per UI-SPEC"
      contains: "moshpit.filters"
  key_links:
    - from: "src/platform/moshpit/components/MoshpitWorkflowPicker.vue"
      to: "reka-ui"
      via: "import { ComboboxRoot, ComboboxInput, ComboboxContent, ComboboxItem, ComboboxAnchor } from 'reka-ui'"
      pattern: "from 'reka-ui'"
    - from: "src/platform/moshpit/components/MoshpitWorkflowPicker.vue"
      to: "src/platform/moshpit/stores/moshpitFilterStore.ts"
      via: "filterStore.setWorkflow"
      pattern: "setWorkflow"
    - from: "src/platform/moshpit/components/MoshpitTimeRangePicker.vue"
      to: "src/platform/moshpit/stores/moshpitFilterStore.ts"
      via: "filterStore.setTimeRange"
      pattern: "setTimeRange"
---

<objective>
Ship the initial-filter-gate UI (FILTER-01) and time-range picker (FILTER-06), plus every `moshpit.filters.*`/`moshpit.sort.*` i18n key needed by downstream Wave 3 plans.

Purpose: Unblock Plans 07/08/09/10 by landing the always-visible Settings-panel controls and the full i18n namespace up-front. After this plan, `filterStore.workflow` and `filterStore.timeRange` can be driven from the UI.

Output: Two Vue 3.5 SFCs using Reka UI primitives only (no new PrimeVue), a supporting composable, component tests with Testing Library, and the complete Phase 3 i18n entry.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/03-filter-sort-core-validation/03-CONTEXT.md
@.planning/phases/03-filter-sort-core-validation/03-UI-SPEC.md
@.planning/phases/03-filter-sort-core-validation/03-RESEARCH.md
@src/platform/moshpit/services/filterTypes.ts
@src/platform/moshpit/services/paramNormalize.ts
@src/platform/moshpit/composables/useMoshpitAssetRegistry.ts
@src/components/ui/search-input/SearchAutocomplete.vue
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add all Phase 3 i18n keys + useMoshpitWorkflowOptions composable</name>
  <files>src/locales/en/main.json, src/platform/moshpit/composables/useMoshpitWorkflowOptions.ts, src/platform/moshpit/composables/useMoshpitWorkflowOptions.test.ts</files>
  <read_first>
    - src/locales/en/main.json (lines 1049-1076 — existing moshpit.* namespaces; extend without breaking)
    - .planning/phases/03-filter-sort-core-validation/03-UI-SPEC.md §Copywriting Contract (authoritative i18n table)
    - src/platform/moshpit/composables/useMoshpitAssetRegistry.ts (entry source)
    - src/platform/moshpit/stores/moshpitMetadataStore.ts (paramsByHash — source of workflowFingerprint)
  </read_first>
  <action>
**A. i18n keys.** Edit `src/locales/en/main.json`. Under the existing `moshpit` object, add two sibling namespaces `filters` and `sort`. Copy keys VERBATIM from UI-SPEC §Copywriting Contract:

`moshpit.filters` must have these 38 keys (exact copy values from UI-SPEC):
sectionLabel, workflowPickerLabel, workflowPickerPlaceholder, workflowPickerSearch, workflowOptionCount ("{name} ({count, number} asset) | {name} ({count, number} assets)"), timeRangeLabel, timeRangeToday ("Today"), timeRangeThisWeek ("This week"), timeRangeThisMonth ("This month"), timeRangeAllTime ("All time"), timeRangeCustom ("Custom…"), timeRangeFrom ("From"), timeRangeTo ("To"), addFilter ("Add filter"), searchParams ("Search parameters…"), removeChip ("Remove {param} filter"), chipCount ("{count} filter active | {count} filters active"), paramModel ("Model"), paramLoras ("LoRA"), paramCfg ("CFG"), paramSteps ("Steps"), paramSampler ("Sampler"), paramScheduler ("Scheduler"), paramSeed ("Seed"), paramPrompt ("Prompt"), paramNegativePrompt ("Negative prompt"), paramResolution ("Resolution"), paramGenerationTime ("Generation time"), paramTags ("Tags"), paramFavourite ("Favourite"), editorApply ("Apply filter"), editorBack ("Back"), editorMin ("Min"), editorMax ("Max"), editorExact ("Exact value"), editorSubstringHint ("Substring match, case-insensitive"), editorSearchValues ("Search values…"), editorFavouriteLabel ("Only favourited assets"), emptyStateHeading ("Pick a workflow to start"), emptyStateBody ("Select a workflow and time range in the Settings panel to populate the canvas.").

`moshpit.sort` must have these 12 keys:
sectionLabel ("Sort"), xAxisLabel ("X"), yAxisLabel ("Y"), xAxisPlaceholder ("Sort by…"), yAxisPlaceholder ("+ Add Y axis"), clearAxis ("Clear {axis} axis"), axisSetAnnouncement ("Sorting by {param} on {axis} axis."), axisClearedAnnouncement ("Sort cleared."), gridSpacingLabel ("Grid spacing"), gridSpacingValue ("{value}px"), gridSpacingDisabledTooltip ("Set a sort axis to enable grid spacing"), showHiddenLabel ("Show hidden").

Do NOT reorder or alter existing moshpit sibling namespaces (workspace, assets, canvas, commands, sidebar).

**B. useMoshpitWorkflowOptions composable** at `src/platform/moshpit/composables/useMoshpitWorkflowOptions.ts`:

```ts
import type { ComputedRef } from 'vue'
import { computed } from 'vue'
import { useMoshpitAssetRegistry } from './useMoshpitAssetRegistry'
import { useMoshpitMetadataStore } from '../stores/moshpitMetadataStore'

export interface WorkflowOption {
  readonly fingerprint: string
  readonly displayName: string
  readonly count: number
}

export function useMoshpitWorkflowOptions(): {
  readonly options: ComputedRef<readonly WorkflowOption[]>
} {
  const registry = useMoshpitAssetRegistry()
  const metaStore = useMoshpitMetadataStore()
  const options = computed<readonly WorkflowOption[]>(() => {
    const counts = new Map<string, number>()
    for (const entry of registry.entries.value) {
      const params = metaStore.paramsByHash.get(entry.contentHash)
      if (!params || !params.workflowFingerprint) continue
      counts.set(params.workflowFingerprint, (counts.get(params.workflowFingerprint) ?? 0) + 1)
    }
    const out: WorkflowOption[] = []
    for (const [fingerprint, count] of counts) {
      out.push({ fingerprint, displayName: fingerprint, count })
    }
    out.sort((a, b) => b.count - a.count || (a.fingerprint < b.fingerprint ? -1 : 1))
    return out
  })
  return { options }
}
```

Tests in `useMoshpitWorkflowOptions.test.ts` (Pinia harness, seed metaStore + stub registry):
- Empty registry → empty options
- Three assets fingerprint 'A', one 'B' → two options; 'A' first (higher count)
- Assets with empty workflowFingerprint excluded
- Assets without paramsByHash entry excluded

Commit: `feat(03-06): add moshpit filters/sort i18n keys + workflow options composable`
  </action>
  <verify>
    <automated>pnpm test:unit --run src/platform/moshpit/composables/useMoshpitWorkflowOptions.test.ts && node -e "const m=require('./src/locales/en/main.json'); const f=['sectionLabel','workflowPickerLabel','workflowPickerPlaceholder','workflowPickerSearch','workflowOptionCount','timeRangeLabel','timeRangeToday','timeRangeThisWeek','timeRangeThisMonth','timeRangeAllTime','timeRangeCustom','timeRangeFrom','timeRangeTo','addFilter','searchParams','removeChip','chipCount','paramModel','paramLoras','paramCfg','paramSteps','paramSampler','paramScheduler','paramSeed','paramPrompt','paramNegativePrompt','paramResolution','paramGenerationTime','paramTags','paramFavourite','editorApply','editorBack','editorMin','editorMax','editorExact','editorSubstringHint','editorSearchValues','editorFavouriteLabel','emptyStateHeading','emptyStateBody'];for(const k of f){if(!(k in m.moshpit.filters))throw new Error('missing filters.'+k)}const s=['sectionLabel','xAxisLabel','yAxisLabel','xAxisPlaceholder','yAxisPlaceholder','clearAxis','axisSetAnnouncement','axisClearedAnnouncement','gridSpacingLabel','gridSpacingValue','gridSpacingDisabledTooltip','showHiddenLabel'];for(const k of s){if(!(k in m.moshpit.sort))throw new Error('missing sort.'+k)}console.log('i18n OK')"</automated>
  </verify>
  <acceptance_criteria>
    - i18n validation script prints `i18n OK` (all 50 keys present)
    - useMoshpitWorkflowOptions.ts exports `useMoshpitWorkflowOptions` and `WorkflowOption`
    - useMoshpitWorkflowOptions.test.ts has ≥ 4 `it(` blocks
    - `pnpm test:unit` passes; `pnpm typecheck` passes
    - Existing moshpit.workspace/assets/canvas/commands/sidebar sub-keys unchanged (verify via git diff review)
  </acceptance_criteria>
  <done>All 50 i18n keys present with exact UI-SPEC copy; useMoshpitWorkflowOptions groups registry entries by workflowFingerprint; tests green; no existing key touched.</done>
</task>

<task type="auto">
  <name>Task 2: Build MoshpitWorkflowPicker and MoshpitTimeRangePicker Vue SFCs</name>
  <files>src/platform/moshpit/components/MoshpitWorkflowPicker.vue, src/platform/moshpit/components/MoshpitWorkflowPicker.test.ts, src/platform/moshpit/components/MoshpitTimeRangePicker.vue, src/platform/moshpit/components/MoshpitTimeRangePicker.test.ts</files>
  <read_first>
    - .planning/phases/03-filter-sort-core-validation/03-UI-SPEC.md (Surfaces 2 and 3 — geometry, typography, a11y)
    - src/components/ui/search-input/SearchAutocomplete.vue (Reka ComboboxRoot reference pattern)
    - src/platform/moshpit/stores/moshpitFilterStore.ts (setWorkflow, setTimeRange actions)
    - src/platform/moshpit/composables/useMoshpitWorkflowOptions.ts (data source)
    - src/platform/moshpit/services/filterTypes.ts (TimeRange, TimePreset, TIME_PRESETS)
    - src/platform/moshpit/components/MoshpitProcessingIndicator.test.ts (Moshpit Vue SFC test pattern)
    - CLAUDE.md / AGENTS.md (Vue 3.5 destructured props; `cn()` via `@/utils/tailwindUtil`; no `dark:`, no `:class="[]"`, no `!important`, no `any`)
  </read_first>
  <action>
**A. MoshpitWorkflowPicker.vue** — per UI-SPEC Surface 2. Use Reka `ComboboxRoot/ComboboxAnchor/ComboboxContent/ComboboxInput/ComboboxItem` (same pattern as `SearchAutocomplete.vue`). Key requirements:

- `<ComboboxRoot :model-value="filterStore.workflow" @update:model-value="(v) => filterStore.setWorkflow(v)">`
- Trigger: `<button type="button">` with `h-8 w-full flex items-center justify-between gap-2 rounded-md border border-border-subtle px-2 text-xs bg-secondary-background`
- Trigger text: `workflow ?? t('moshpit.filters.workflowPickerPlaceholder')` with class `text-muted-foreground` when null, `text-base-foreground` when set
- Trigger aria-label: `t('moshpit.filters.workflowPickerLabel')`
- Trigger `data-testid="moshpit-workflow-picker-trigger"`
- Chevron icon: `<i class="icon-[lucide--chevron-down] size-3.5" aria-hidden="true" />`
- `<ComboboxContent position="popper">` with classes `z-1700 max-h-60 w-[var(--reka-combobox-trigger-width)] overflow-y-auto rounded-md border border-border-subtle bg-interface-panel-surface shadow-interface`
- `<ComboboxInput :placeholder="t('moshpit.filters.workflowPickerSearch')" :aria-label="t('moshpit.filters.workflowPickerSearch')" class="sticky top-0 h-8 w-full border-b border-border-subtle bg-transparent px-2 text-xs outline-none" />`
- Loop `<ComboboxItem v-for="opt in options" :key="opt.fingerprint" :value="opt.fingerprint">` with class `flex h-10 cursor-pointer items-center justify-between px-2 text-xs hover:bg-secondary-background-hover`
- Item content: `<span class="truncate text-base-foreground">{{ opt.displayName }}</span><span class="text-muted-foreground" aria-hidden="true"> ({{ n(opt.count) }} {{ opt.count === 1 ? 'asset' : 'assets' }})</span>` — OR use `t('moshpit.filters.workflowOptionCount', { name: opt.displayName, count: opt.count }, opt.count)` which is the ICU plural pattern; prefer the ICU plural version as it's the authored i18n key
- Script setup imports: `{ ComboboxAnchor, ComboboxContent, ComboboxInput, ComboboxItem, ComboboxRoot } from 'reka-ui'`, `{ computed } from 'vue'`, `{ useI18n } from 'vue-i18n'`, `{ useMoshpitWorkflowOptions } from '@/platform/moshpit/composables/useMoshpitWorkflowOptions'`, `{ useMoshpitFilterStore } from '@/platform/moshpit/stores/moshpitFilterStore'`, `{ cn } from '@/utils/tailwindUtil'`
- `defineOptions({ name: 'MoshpitWorkflowPicker' })`
- No props, no emits — entirely store-driven

Tests (`MoshpitWorkflowPicker.test.ts`) using `@testing-library/vue` + `@testing-library/user-event`:
- Renders placeholder "Select a workflow…" when filterStore.workflow === null (findByText)
- Renders the selected fingerprint value when setWorkflow called
- Clicking trigger opens the combobox (ComboboxContent rendered / findByRole('listbox'))
- Clicking an option calls filterStore.setWorkflow with that fingerprint (watch the store's workflow ref)
- Uses i18n test harness — import `createI18n` and mount with the real en/main.json locale messages (NOT mock strings), per vue-i18n testing pattern in existing codebase

**B. MoshpitTimeRangePicker.vue** — per UI-SPEC Surface 3. Key requirements:

- Root `<div class="flex flex-col gap-2">`
- Section label `<span class="text-2xs uppercase tracking-wide text-muted-foreground">{{ t('moshpit.filters.timeRangeLabel') }}</span>`
- Pill group: `<div class="flex flex-wrap gap-1" role="radiogroup" :aria-label="t('moshpit.filters.timeRangeLabel')">`
- Pill buttons: iterate `TIME_PRESETS` from filterTypes, each is `<button type="button" role="radio" :aria-checked="timeRange.preset === preset" class="h-6 rounded-full px-2 text-xs ...">`
- Active pill class: `bg-secondary-background-selected text-base-foreground`
- Inactive pill class: `bg-secondary-background text-muted-foreground hover:bg-secondary-background-hover`
- Pill label i18n map: today→timeRangeToday, thisWeek→timeRangeThisWeek, thisMonth→timeRangeThisMonth, all→timeRangeAllTime, custom→timeRangeCustom
- Custom range: `<div v-if="timeRange.preset === 'custom'">` with two `<label>` rows each containing a fixed-width label (`w-10 text-2xs uppercase tracking-wide text-muted-foreground`) and `<input type="date" class="h-8 flex-1 rounded-md border border-border-subtle bg-transparent px-2 text-xs text-base-foreground">`
- Date conversion: `epochToISODate(epochMs) → 'YYYY-MM-DD'`, `isoDateToEpoch('YYYY-MM-DD') → epoch ms at 00:00:00Z or null if invalid`
- Clicking a preset pill calls `filterStore.setTimeRange({ preset, from: null, to: null })`
- Typing into "From" input calls `filterStore.setTimeRange({ preset: 'custom', from: isoDateToEpoch(value), to: filterStore.timeRange.to })`
- Typing into "To" input mirrors — preset='custom', updates `to`
- Aria-labels on date inputs use `t('moshpit.filters.timeRangeFrom')` / `t('moshpit.filters.timeRangeTo')`
- No PrimeVue imports (verify with grep — acceptance criterion below)

Tests (`MoshpitTimeRangePicker.test.ts`):
- Renders 5 pills with accessible names matching i18n'd preset labels
- Active pill has `aria-checked="true"`; others `aria-checked="false"`
- Clicking "Today" calls setTimeRange with exactly `{ preset: 'today', from: null, to: null }`
- Clicking "Custom…" reveals the From/To input pair
- When preset is NOT 'custom', From/To inputs are NOT rendered in DOM
- Typing '2026-04-21' into From input → setTimeRange called with `{ preset: 'custom', from: Date.UTC(2026, 3, 21), to: null }`
- Typing an invalid date string → from stays at previous value (null)

**Constraints across both components (verify):**
- No `:class="[]"` — use `cn()`
- No `dark:` variants
- No `any` / `as any`
- No `primevue/` imports
- No `!important`
- All user-facing strings via `t(...)` from `useI18n`

Commit: `feat(03-06): add MoshpitWorkflowPicker + MoshpitTimeRangePicker (FILTER-01, FILTER-06)`
  </action>
  <verify>
    <automated>pnpm test:unit --run src/platform/moshpit/components/MoshpitWorkflowPicker.test.ts src/platform/moshpit/components/MoshpitTimeRangePicker.test.ts && pnpm typecheck && pnpm lint src/platform/moshpit/components/MoshpitWorkflowPicker.vue src/platform/moshpit/components/MoshpitTimeRangePicker.vue</automated>
  </verify>
  <acceptance_criteria>
    - Both SFCs exist; both test files have ≥ 5 `it(` blocks each
    - `grep -l "primevue" src/platform/moshpit/components/MoshpitWorkflowPicker.vue src/platform/moshpit/components/MoshpitTimeRangePicker.vue` returns NO matches
    - `grep -l "dark:" src/platform/moshpit/components/MoshpitWorkflowPicker.vue src/platform/moshpit/components/MoshpitTimeRangePicker.vue` returns NO matches
    - `grep -lE ":class=\"\\[" src/platform/moshpit/components/MoshpitWorkflowPicker.vue src/platform/moshpit/components/MoshpitTimeRangePicker.vue` returns NO matches
    - `grep -E "\\bany\\b|as any" src/platform/moshpit/components/MoshpitWorkflowPicker.vue src/platform/moshpit/components/MoshpitTimeRangePicker.vue` returns NO matches
    - `grep "from 'reka-ui'" src/platform/moshpit/components/MoshpitWorkflowPicker.vue` returns a match (uses Reka combobox)
    - `grep "setWorkflow" src/platform/moshpit/components/MoshpitWorkflowPicker.vue` returns a match
    - `grep "setTimeRange" src/platform/moshpit/components/MoshpitTimeRangePicker.vue` returns a match
    - `grep 'type="date"' src/platform/moshpit/components/MoshpitTimeRangePicker.vue` returns ≥ 2 matches (from + to)
    - `pnpm test:unit` + `pnpm typecheck` + `pnpm lint` all exit 0
    - Commit matches `^feat\(03-06\):`
  </acceptance_criteria>
  <done>Two Reka-driven Vue 3.5 SFCs, no PrimeVue, no dark:, no :class=[], no any; tests prove filterStore interactions; lint + typecheck clean.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| User date input → filterStore | User-entered `type="date"` string parsed via `new Date(iso + 'T00:00:00Z').getTime()` then validated with `Number.isFinite` |
| Workflow option list → store | Options derived from already-validated NormalizedParams.workflowFingerprint; no user-typed input reaches the store value |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-06-01 | Tampering | Malformed date string produces NaN epoch | mitigate | `isoDateToEpoch` returns null on NaN; setTimeRange preserves previous valid from/to when one side becomes null |
| T-03-06-02 | XSS | Workflow fingerprint displayed in combobox | mitigate | Rendered via Vue text interpolation only (`{{ opt.displayName }}`) — Vue auto-escapes. No `v-html`. |
| T-03-06-03 | Information Disclosure | Workflow fingerprint reveals workflow shape | accept | Local-only display; no telemetry or network egress in v1 (per D-21, no Playwright fixture blocking; per CONTEXT.md no telemetry) |
</threat_model>

<verification>
```
pnpm test:unit --run src/platform/moshpit/components/MoshpitWorkflowPicker.test.ts src/platform/moshpit/components/MoshpitTimeRangePicker.test.ts src/platform/moshpit/composables/useMoshpitWorkflowOptions.test.ts
pnpm typecheck
pnpm lint
```
</verification>

<success_criteria>
- All 50 i18n keys from UI-SPEC present under moshpit.filters / moshpit.sort
- useMoshpitWorkflowOptions derives workflow groupings from paramsByHash
- MoshpitWorkflowPicker uses Reka ComboboxRoot; no PrimeVue; wires to setWorkflow
- MoshpitTimeRangePicker renders 5 preset pills + custom date inputs; no PrimeVue; wires to setTimeRange
- Both components obey CLAUDE.md constraints (no dark:, no :class=[], no any, no !important)
- All tests green; typecheck + lint clean
</success_criteria>

<output>
Create `.planning/phases/03-filter-sort-core-validation/03-06-SUMMARY.md` documenting:
- Any i18n keys added beyond the UI-SPEC list (should be zero)
- Workflow option display strategy (fingerprint-as-name per OQ-3)
- Integration note for Plan 03-10: MoshpitSettingsPanel will mount these two components at the top of the panel body
</output>

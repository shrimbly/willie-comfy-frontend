---
phase: 04
plan: 06
type: execute
wave: 4
depends_on: [3, 4, 5]
files_modified:
  - src/platform/moshpit/components/MoshpitSettingsPanel.vue
  - src/platform/moshpit/components/MoshpitSettingsPanel.test.ts
  - src/platform/moshpit/components/MoshpitCanvas.vue
  - src/platform/moshpit/components/MoshpitCanvas.test.ts
  - src/platform/moshpit/components/MoshpitGridSpacingControl.vue
  - src/locales/en/main.json
  - .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md
  - .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md
  - browser_tests/tests/moshpit/lineage-groupings.spec.ts
autonomous: false
requirements:
  - GROUP-01
  - GROUP-02
  - GROUP-03
  - GROUP-04
  - GROUP-05
  - GROUP-06
  - GROUP-07
  - GROUP-08
  - GROUP-09
  - GROUP-10
  - CSORT-01
  - FILTER-12
must_haves:
  truths:
    - "MoshpitSettingsPanel renders the new layout: initial gate (workflow + time) → MoshpitGroupingToggles → MoshpitFilterChipRow(tier=primary) → MoshpitWithinClusterSort → MoshpitAdvancedFilters → MoshpitGridSpacingControl → MoshpitShowHiddenToggle → excluded-count row"
    - "MoshpitSettingsPanel no longer mounts MoshpitSortControls (file was already deleted by Plan 03 Task 4 — this plan just adds the new composition)"
    - "MoshpitCanvas mounts MoshpitClusterOverlay (Plan 05 component) in place of MoshpitAxisOverlay (file was already deleted by Plan 03 Task 4 — this plan adds the new overlay; NOTE: Plan 03 SUMMARY flagged that the axis overlay actually lived in src/views/MoshpitView.vue; the executor must confirm the correct mount location for the cluster overlay before editing)"
    - "MoshpitSortControls.vue + MoshpitSortControls.test.ts + MoshpitAxisOverlay.vue + MoshpitAxisOverlay.test.ts are ABSENT from the filesystem (deletion originated in Plan 03 Task 4; this plan verifies the absence holds)"
    - "moshpit.sort.* i18n keys are removed from src/locales/en/main.json; JSON remains valid"
    - "MoshpitGridSpacingControl label text now refers to cluster+within-cluster spacing (via moshpit.grouping.spacingLabel)"
    - "04-HUMAN-UAT.md exists at the phase directory with D-20 dogfood checklist and D-22 gate statement"
    - "04-VALIDATION.md per-task verification map is populated with real task IDs + paths + commands; nyquist_compliant frontmatter set to true"
    - "A @moshpit Playwright spec at browser_tests/tests/moshpit/lineage-groupings.spec.ts exercises grouping toggle state + advanced disclosure (best-effort, not a blocker for acceptance per D-21)"
  artifacts:
    - path: ".planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md"
      provides: "Dogfood checklist implementing D-20"
      min_lines: 40
    - path: "browser_tests/tests/moshpit/lineage-groupings.spec.ts"
      provides: "Playwright spec (tag @moshpit) covering GROUP-01 + FILTER-12 UI states"
      min_lines: 40
  key_links:
    - from: "MoshpitSettingsPanel.vue"
      to: "MoshpitGroupingToggles + MoshpitWithinClusterSort + MoshpitAdvancedFilters + MoshpitFilterChipRow"
      via: "template mounts each in the documented order"
      pattern: "MoshpitGroupingToggles.*MoshpitWithinClusterSort.*MoshpitAdvancedFilters"
    - from: "MoshpitCanvas.vue"
      to: "MoshpitClusterOverlay"
      via: "template replaces MoshpitAxisOverlay mount"
      pattern: "MoshpitClusterOverlay"
---

<objective>
Compose all Phase 4 pieces into the Settings panel + canvas, finish the i18n cleanup (key removal; file deletions were handled by Plan 03 Task 4), update the grid-spacing label, write the HUMAN-UAT checklist (the v3 Core Value gate per D-22), populate the VALIDATION map, and — best-effort — ship a @moshpit Playwright spec for grouping-toggle state + advanced disclosure.

This is the integration plan: everything in Plans 01-05 is implemented and individually tested; Plan 06 wires them together so the user can see, use, and sign off on the phase.

Purpose: Close the Phase 4 loop. After this plan:
- Launching Moshpit shows grouping pills, within-cluster sort, primary chips, advanced disclosure, grid spacing, show-hidden toggle (no sort controls)
- The canvas renders cluster bounding boxes + labels on pan/zoom
- The old sort UI is fully gone from code + i18n (components deleted in Plan 03 Task 4; i18n keys deleted here)
- HUMAN-UAT exists and is the gating checklist for whether Phase 5 starts
- VALIDATION.md is frontmatter-compliant

Output: Settings panel composition + canvas overlay swap + i18n cleanup + grid-spacing label update + HUMAN-UAT.md + VALIDATION.md update + optional Playwright spec. One `checkpoint:human-verify` task at the end for the user to sign off before committing the phase as complete.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-RESEARCH.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-01-PLAN.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-02-PLAN.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-03-PLAN.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-04-PLAN.md
@.planning/phases/04-lineage-groupings-within-cluster-sort/04-05-PLAN.md

@src/platform/moshpit/components/MoshpitSettingsPanel.vue
@src/platform/moshpit/components/MoshpitCanvas.vue
@src/platform/moshpit/components/MoshpitGridSpacingControl.vue

<interfaces>
<!-- Components shipped by Plans 04 + 05 (ready to consume) -->
```typescript
// Plan 04:
import MoshpitGroupingToggles from './MoshpitGroupingToggles.vue'
import MoshpitWithinClusterSort from './MoshpitWithinClusterSort.vue'
import MoshpitAdvancedFilters from './MoshpitAdvancedFilters.vue'

// MoshpitFilterChipRow now accepts: tier?: 'primary' | 'advanced'
// MoshpitAddFilterPopover now shows Primary / Advanced sections + saveNode entry

// Plan 05:
import MoshpitClusterOverlay from './MoshpitClusterOverlay.vue'
```

<!-- moshpit i18n namespace after Plan 04 -->
```
moshpit.grouping.sectionLabel / axis.* / withinSort.* / otherLabel / spacingLabel  (ADDED by Plan 04)
moshpit.filters.primaryLabel / advancedLabel / paramSaveNode                        (ADDED by Plan 04)
moshpit.sort.*                                                                       (STILL PRESENT; this plan REMOVES)
```

<!-- Filesystem state at start of Plan 06 (post Plan 03 Task 4) -->
```
# DELETED by Plan 03 Task 4 — this plan verifies they are still absent:
src/platform/moshpit/components/MoshpitSortControls.vue        → absent
src/platform/moshpit/components/MoshpitSortControls.test.ts    → absent
src/platform/moshpit/components/MoshpitAxisOverlay.vue         → absent
src/platform/moshpit/components/MoshpitAxisOverlay.test.ts     → absent
# EDITED by Plan 03 Task 4 — imports/mounts removed:
src/platform/moshpit/components/MoshpitSettingsPanel.vue       → MoshpitSortControls reference gone
src/views/MoshpitView.vue                                      → MoshpitAxisOverlay reference gone
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Recompose MoshpitSettingsPanel — new layout, mount grouping/within-sort/advanced components</name>
  <files>
    src/platform/moshpit/components/MoshpitSettingsPanel.vue,
    src/platform/moshpit/components/MoshpitSettingsPanel.test.ts
  </files>
  <read_first>
    - src/platform/moshpit/components/MoshpitSettingsPanel.vue (full file — current structure post Plan 03 Task 4: no MoshpitSortControls mount/import; filter gate + excluded count still present)
    - src/platform/moshpit/components/MoshpitSettingsPanel.test.ts (post Plan 03 Task 4: MoshpitSortControls assertions already stripped; baseline smoke test remains; this plan adds the new-layout assertions)
    - src/platform/moshpit/components/MoshpitGroupingToggles.vue (from Plan 04 Task 1)
    - src/platform/moshpit/components/MoshpitWithinClusterSort.vue (from Plan 04 Task 2)
    - src/platform/moshpit/components/MoshpitAdvancedFilters.vue (from Plan 04 Task 3)
    - src/platform/moshpit/components/MoshpitFilterChipRow.vue (from Plan 04 Task 4 — now accepts `tier` prop)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md (Integration Points section: exact layout order — "initial filter gate → grouping toggles → primary chip row → within-cluster sort dropdown → Advanced disclosure → grid spacing → show-hidden → excluded count")
  </read_first>
  <action>
Rewrite the `<template>` section of `MoshpitSettingsPanel.vue` to match the target layout order. Preserve the outer `<aside>` + `<header>` + scrollable content wrapper. Replace the inner content with:

```vue
<div class="flex-1 overflow-y-auto px-3 py-2 text-xs">
  <!-- Initial filter gate (unchanged) -->
  <section class="flex flex-col gap-2" :aria-label="t('moshpit.filters.sectionLabel')">
    <span class="text-2xs tracking-wide text-muted-foreground uppercase">
      {{ t('moshpit.filters.sectionLabel') }}
    </span>
    <MoshpitWorkflowPicker />
    <MoshpitTimeRangePicker />
  </section>

  <!-- Grouping toggles (NEW — D-18, appears once gated) -->
  <MoshpitGroupingToggles v-if="filterStore.isGated" class="mt-4" />

  <!-- Primary chip row (tier=primary) -->
  <div v-if="filterStore.isGated" class="mt-4">
    <MoshpitFilterChipRow tier="primary" />
  </div>

  <!-- Within-cluster sort dropdown (CSORT-01) -->
  <MoshpitWithinClusterSort v-if="filterStore.isGated" class="mt-4" />

  <!-- Advanced filter disclosure (FILTER-12) -->
  <MoshpitAdvancedFilters v-if="filterStore.isGated" class="mt-4" />

  <!-- Footer controls (retained: SORT-04 cluster spacing + FILTER-11 show-hidden) -->
  <div class="mt-4 flex flex-col gap-0">
    <MoshpitGridSpacingControl />
    <MoshpitShowHiddenToggle />
  </div>

  <!-- Excluded-count row (ASSET-06 unchanged) -->
  <div
    v-if="excludedCount > 0"
    class="mt-2 flex items-center gap-1"
    data-testid="moshpit-excluded-count"
  >
    <span :id="excludedDescId" class="text-muted-foreground">
      {{ t('moshpit.assets.excludedCount', excludedCount) }}
    </span>
    <i
      v-tooltip.top="t('moshpit.assets.excludedTooltip')"
      class="icon-[lucide--info] size-3.5 cursor-help text-muted-foreground"
      role="img"
      :aria-label="t('moshpit.assets.excludedTooltipLabel')"
      :aria-describedby="excludedDescId"
    />
  </div>
</div>
```

Update the `<script setup>` imports:
- (The old `import MoshpitSortControls from ...` line should already be gone — removed by Plan 03 Task 4. Verify with grep before editing; if somehow still present, remove it now.)
- ADD: `import MoshpitGroupingToggles from '@/platform/moshpit/components/MoshpitGroupingToggles.vue'`
- ADD: `import MoshpitWithinClusterSort from '@/platform/moshpit/components/MoshpitWithinClusterSort.vue'`
- ADD: `import MoshpitAdvancedFilters from '@/platform/moshpit/components/MoshpitAdvancedFilters.vue'`
- KEEP: `MoshpitFilterChipRow`, `MoshpitGridSpacingControl`, `MoshpitShowHiddenToggle`, `MoshpitTimeRangePicker`, `MoshpitWorkflowPicker` imports
- KEEP: `filterStore`, `metadataStore`, `excludedCount`, `excludedDescId` declarations

Rewrite `MoshpitSettingsPanel.test.ts`:
- (Assertions referencing `MoshpitSortControls`, `sortX`, `sortY` should already be gone — stripped by Plan 03 Task 4. Verify with grep.)
- ADD layout-order assertions: mount gated (stubbed filterStore.isGated = true), query for each new component by data-testid / role, assert their DOM order matches the template above (grouping toggles → primary chip row → within-cluster sort → advanced disclosure → grid spacing → show hidden → excluded count)
- ADD: when `filterStore.isGated === false`, only `WorkflowPicker` + `TimeRangePicker` render; none of the grouping / chip / within-sort / advanced components are present
- KEEP existing excluded-count and a11y tests
  </action>
  <verify>
    <automated>pnpm test:unit -- src/platform/moshpit/components/MoshpitSettingsPanel.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "MoshpitSortControls" src/platform/moshpit/components/MoshpitSettingsPanel.vue` returns 0 hits
    - `grep -n "MoshpitGroupingToggles\|MoshpitWithinClusterSort\|MoshpitAdvancedFilters" src/platform/moshpit/components/MoshpitSettingsPanel.vue` returns ≥3 hits (each mounted)
    - `grep -n "tier=\"primary\"" src/platform/moshpit/components/MoshpitSettingsPanel.vue` returns 1 hit
    - `grep -n "MoshpitFilterChipRow" src/platform/moshpit/components/MoshpitSettingsPanel.vue` returns 1 hit (primary tier only; advanced lives inside MoshpitAdvancedFilters)
    - `grep -n "MoshpitSortControls" src/platform/moshpit/components/MoshpitSettingsPanel.test.ts` returns 0 hits
    - `pnpm test:unit -- src/platform/moshpit/components/MoshpitSettingsPanel.test.ts --run` exits 0
  </acceptance_criteria>
  <done>
    Settings panel mounts the new composition in the documented order; no trace of the old sort section; tests green.
  </done>
</task>

<task type="auto">
  <name>Task 2: Mount MoshpitClusterOverlay in place of the removed MoshpitAxisOverlay</name>
  <files>
    src/platform/moshpit/components/MoshpitCanvas.vue,
    src/platform/moshpit/components/MoshpitCanvas.test.ts
  </files>
  <read_first>
    - src/platform/moshpit/components/MoshpitCanvas.vue (full file — note: Plan 03 SUMMARY flagged that the original MoshpitAxisOverlay mount actually lived in src/views/MoshpitView.vue, not MoshpitCanvas.vue. BEFORE editing, grep both files to determine the correct target for the cluster overlay mount. If MoshpitCanvas.vue never mounted the axis overlay, the cluster overlay likely belongs in MoshpitView.vue instead — adjust this task's target file accordingly and update files_modified.)
    - src/views/MoshpitView.vue (post Plan 03 Task 4: MoshpitAxisOverlay import/mount gone. This may be the correct home for MoshpitClusterOverlay.)
    - src/platform/moshpit/components/MoshpitCanvas.test.ts (existing coverage; identify tests that reference MoshpitAxisOverlay — should already be none since the original overlay lived in MoshpitView, not MoshpitCanvas)
    - src/platform/moshpit/components/MoshpitClusterOverlay.vue (from Plan 05 — note the injection key / viewport plumbing it needs; mirror wherever the sprite layer lives so both share the same transform)
    - src/platform/moshpit/composables/useMoshpitViewportInjection.ts (ensure the injection key + provider pattern is unchanged — MoshpitClusterOverlay uses the same key)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md (Integration Points: "<MoshpitClusterOverlay> parallel to the existing sprite layer (remove <MoshpitAxisOverlay>)")
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-03-SUMMARY.md (Plan 03 executor's note on correct mount location)
  </read_first>
  <action>
1. Determine the correct parent for MoshpitClusterOverlay:
   - Run `grep -rn "MoshpitSpriteLayer\|useMoshpitSpriteLayer" src/platform/moshpit src/views` to find where the sprite layer is mounted. The cluster overlay must live alongside the sprite layer so both share the viewport transform.
   - If the sprite layer lives in `MoshpitCanvas.vue` → mount `<MoshpitClusterOverlay />` there (this task's listed file is correct).
   - If the sprite layer lives in `src/views/MoshpitView.vue` → update this task: add MoshpitView.vue to the edit target, keep MoshpitCanvas.vue in `files_modified` only if it actually needs touching.

2. In the correct parent component:
   - Add `<MoshpitClusterOverlay />` in the positional slot where the axis overlay previously lived (or parallel to the sprite layer). It should live inside the canvas container, positioned absolutely over the PixiJS canvas, consuming the same injection-provided viewport. No `v-if` gating needed — MoshpitClusterOverlay internally handles the null-tree case (Plan 05 Task 1 behaviour).
   - Add `import MoshpitClusterOverlay from '@/platform/moshpit/components/MoshpitClusterOverlay.vue'`.

3. Update the corresponding test file:
   - Any old assertion referencing `MoshpitAxisOverlay` should already be absent (Plan 03 Task 4 stripped them if they were in MoshpitSettingsPanel; MoshpitCanvas.test.ts was not in Plan 03 Task 4's edit list — re-grep to verify).
   - ADD a test asserting `MoshpitClusterOverlay` is present in the mounted DOM (via `data-testid="moshpit-cluster-overlay"`).
   - KEEP existing sprite-layer / pan-zoom / selection tests.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/platform/moshpit/components/MoshpitCanvas.test.ts --run</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "MoshpitAxisOverlay" src/` returns 0 hits (already confirmed by Plan 03 Task 4; this plan re-verifies)
    - `grep -rn "MoshpitClusterOverlay" src/platform/moshpit src/views` returns ≥2 hits (import + template, in whichever parent owns the sprite layer)
    - `grep -n "moshpit-cluster-overlay" src/platform/moshpit/components/MoshpitCanvas.test.ts` returns ≥1 hit (or the test file of whichever parent was edited)
    - `pnpm test:unit -- src/platform/moshpit/components/MoshpitCanvas.test.ts --run` exits 0
  </acceptance_criteria>
  <done>
    Cluster overlay is mounted parallel to the sprite layer; axis overlay references are gone; test green.
  </done>
</task>

<task type="auto">
  <name>Task 3: Verify prior sort-UI deletion holds + remove moshpit.sort.* i18n keys + knip cleanup</name>
  <files>
    src/locales/en/main.json
  </files>
  <read_first>
    - src/locales/en/main.json (moshpit.sort.* keys at lines 1120-1133 per RESEARCH — remove that entire block. Note: Plan 04 added moshpit.grouping.* and moshpit.filters.primaryLabel / advancedLabel / paramSaveNode; DO NOT remove those.)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-03-SUMMARY.md (confirms which files Plan 03 Task 4 deleted — this task verifies absence, does NOT re-delete)
    - browser_tests/tests/moshpit/ (if any Playwright spec references moshpit-sort-*, note it — Task 5 below either updates or removes those references)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md (D-15 deletion list + D-16 retention list)
  </read_first>
  <action>
**Scope note:** The file deletions of MoshpitSortControls.* and MoshpitAxisOverlay.* originated in Plan 03 Task 4 (not here) so the store refactor in Plan 03 Task 2 could land with a green `pnpm typecheck` — no CLAUDE.md quality-gate bypass. This task's residual work is:
1. Verify the deletions from Plan 03 Task 4 still hold (portable existence tests, not brittle string-matching on `ls` output)
2. Remove `moshpit.sort.*` i18n keys (Plan 03 Task 4 did not touch i18n — intentional, because Plan 04 Task 1 adds `moshpit.grouping.spacingLabel` which this plan's Task 4 wires into MoshpitGridSpacingControl; removing the old `moshpit.sort.gridSpacingLabel` earlier would have broken the control between Plans 03 and 06)
3. Run knip to catch any orphaned exports left behind by the deletions

1. Verify the four doomed component files are still absent (portable POSIX shell existence tests — no `grep -q "No such file"`):
```bash
[ ! -f src/platform/moshpit/components/MoshpitSortControls.vue ] || { echo "FAIL: MoshpitSortControls.vue resurrected"; exit 1; }
[ ! -f src/platform/moshpit/components/MoshpitSortControls.test.ts ] || { echo "FAIL: MoshpitSortControls.test.ts resurrected"; exit 1; }
[ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.vue ] || { echo "FAIL: MoshpitAxisOverlay.vue resurrected"; exit 1; }
[ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.test.ts ] || { echo "FAIL: MoshpitAxisOverlay.test.ts resurrected"; exit 1; }
```
If any assertion fails, the Plan 03 Task 4 deletion regressed — STOP and investigate before proceeding. Do not silently re-delete.

2. Also check for any co-located stories that may exist now and must be absent:
```bash
[ ! -f src/platform/moshpit/components/MoshpitSortControls.stories.ts ] || rm src/platform/moshpit/components/MoshpitSortControls.stories.ts
[ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.stories.ts ] || rm src/platform/moshpit/components/MoshpitAxisOverlay.stories.ts
```

3. Run a global ripgrep to catch any lingering references (Storybook files, route stubs, barrel files):
```bash
grep -rn "MoshpitSortControls\|MoshpitAxisOverlay" src/ browser_tests/ .storybook/ 2>/dev/null
```
Expected output: 0 hits. If any hit appears, clean it up in this task.

4. Edit `src/locales/en/main.json`:
   - Remove the entire `"sort": { ... }` block under `moshpit` (lines ~1120-1133 of the current file — re-verify line numbers before editing).
   - Preserve the surrounding JSON structure: the comma handling on the adjacent siblings must remain valid JSON after removal. After edit, the `moshpit` block should have `workspace`, `assets`, `canvas`, `commands`, `sidebar`, `filters`, `grouping` keys (the 7 that survive; sort is gone).
   - Validate: `node -e "JSON.parse(require('fs').readFileSync('src/locales/en/main.json'))"` must exit 0.

5. Run `pnpm typecheck` — expected to remain green (Plan 03 Task 4 already restored it; removing i18n keys should not regress).

6. Run `pnpm knip` to confirm no unused exports or files leak. If knip flags items related to the deleted components (e.g. a removed export still referenced somewhere), clean them up.
  </action>
  <verify>
    <automated>[ ! -f src/platform/moshpit/components/MoshpitSortControls.vue ] && [ ! -f src/platform/moshpit/components/MoshpitSortControls.test.ts ] && [ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.vue ] && [ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.test.ts ] && node -e "JSON.parse(require('fs').readFileSync('src/locales/en/main.json'))" && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `[ ! -f src/platform/moshpit/components/MoshpitSortControls.vue ]` succeeds (absence confirmed)
    - `[ ! -f src/platform/moshpit/components/MoshpitSortControls.test.ts ]` succeeds
    - `[ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.vue ]` succeeds
    - `[ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.test.ts ]` succeeds
    - `[ ! -f src/platform/moshpit/components/MoshpitSortControls.stories.ts ]` succeeds
    - `[ ! -f src/platform/moshpit/components/MoshpitAxisOverlay.stories.ts ]` succeeds
    - `grep -rn "MoshpitSortControls\|MoshpitAxisOverlay" src/ browser_tests/ .storybook/ 2>/dev/null` returns 0 hits
    - `grep -n "\"sort\":" src/locales/en/main.json` returns 0 hits within the moshpit block (outside of moshpit, other namespaces may legitimately have a "sort" key — only check within the moshpit block; easiest check: `node -e "const m=JSON.parse(require('fs').readFileSync('src/locales/en/main.json')).moshpit; if (m.sort) { console.error('FAIL: moshpit.sort still present'); process.exit(1); }"`)
    - `node -e "JSON.parse(require('fs').readFileSync('src/locales/en/main.json'))"` exits 0
    - `pnpm typecheck` exits 0
    - `pnpm knip` exits 0 (or flags only pre-existing items unrelated to the removed sort UI)
  </acceptance_criteria>
  <done>
    Deprecated-file absence is verified with portable shell tests; moshpit.sort.* i18n keys are gone; typecheck + knip are clean.
  </done>
</task>

<task type="auto">
  <name>Task 4: Update MoshpitGridSpacingControl label to moshpit.grouping.spacingLabel</name>
  <files>
    src/platform/moshpit/components/MoshpitGridSpacingControl.vue
  </files>
  <read_first>
    - src/platform/moshpit/components/MoshpitGridSpacingControl.vue (find where it calls `t('moshpit.sort.gridSpacingLabel')` — that key is now DELETED by Task 3)
    - src/locales/en/main.json (confirm moshpit.grouping.spacingLabel was added by Plan 04 Task 1)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md (D-16 "MoshpitGridSpacingControl retained; label updated (moshpit.grouping.spacingLabel) but mechanism unchanged")
  </read_first>
  <action>
Replace any occurrence of `'moshpit.sort.gridSpacingLabel'` with `'moshpit.grouping.spacingLabel'` inside `MoshpitGridSpacingControl.vue`. If the component references `moshpit.sort.gridSpacingValue`, `moshpit.sort.gridSpacingDisabledTooltip`, or `moshpit.sort.showHiddenLabel`:
- `gridSpacingValue` → keep the text rendering logic but move the key to `moshpit.grouping.spacingValue` and add that key to the `moshpit.grouping.*` block in `src/locales/en/main.json` (value: `"{value}px"`).
- `gridSpacingDisabledTooltip` → the old behaviour disabled the slider when no sort axis was set. With groupings, the slider is ALWAYS enabled (cluster layout runs whether or not groupings are active — empty groupings = single leaf cluster which still uses grid spacing). REMOVE the disabled-tooltip branch entirely. If the disabled state was a prop or computed, replace with constant `false` or remove.
- `showHiddenLabel` → DO NOT move this to the grouping namespace if the key is consumed by `MoshpitShowHiddenToggle.vue`. Instead, create `moshpit.filters.showHiddenLabel` (if that doesn't already exist) and update `MoshpitShowHiddenToggle.vue` to reference it. Verify what MoshpitShowHiddenToggle currently uses — most likely it already has its own `moshpit.filters.*` key.

If `MoshpitShowHiddenToggle.vue` references a `moshpit.sort.*` key, update it in the same commit. Add the file to `files_modified` if that happens.

If `MoshpitGridSpacingControl.test.ts` exists and references the old keys, update its i18n fixture / assertions.
  </action>
  <verify>
    <automated>pnpm test:unit -- src/platform/moshpit/components/MoshpitGridSpacingControl.test.ts --run && pnpm typecheck</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "moshpit.sort" src/platform/moshpit/components/MoshpitGridSpacingControl.vue` returns 0 hits
    - `grep -n "moshpit.grouping.spacingLabel" src/platform/moshpit/components/MoshpitGridSpacingControl.vue` returns 1 hit
    - `grep -rn "moshpit.sort" src/` returns 0 hits (project-wide i18n key is fully removed)
    - `pnpm test:unit -- src/platform/moshpit/components/MoshpitGridSpacingControl.test.ts --run` exits 0 (or passes if no such test file exists — the ls check below)
    - `pnpm typecheck` exits 0
  </acceptance_criteria>
  <done>
    Grid-spacing control label lives in the grouping namespace; no stale moshpit.sort.* references remain anywhere.
  </done>
</task>

<task type="auto">
  <name>Task 5: Write @moshpit Playwright spec for grouping + advanced disclosure</name>
  <files>
    browser_tests/tests/moshpit/lineage-groupings.spec.ts
  </files>
  <read_first>
    - browser_tests/tests/moshpit/ (any existing Playwright specs — reuse helpers, fixtures, page object patterns; check for a moshpit fixture file)
    - browser_tests/fixtures/ComfyPage.ts (check for `comfyPageFixture` base — per .oxlintrc.json tests must `import { comfyPageFixture as test }`)
    - .claude/skills/writing-playwright-tests/SKILL.md (project-specific Playwright patterns)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md (D-21: "No Playwright E2E fixture blocks Phase 4 acceptance. A @moshpit spec may cover grouping-toggle state, auto-nesting order stability under filter changes, and the Advanced disclosure")
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md (Wave 0 list includes `browser_tests/tests/moshpit/lineage-groupings.spec.ts`)
  </read_first>
  <action>
Create `browser_tests/tests/moshpit/lineage-groupings.spec.ts`. Tag the suite with `@moshpit`. Use `comfyPageFixture as test` (required by project lint rule).

Scope (keep it minimal — this is not a blocker per D-21):

1. **Grouping toggle state** — Open Moshpit, apply an initial gate (all workflows / all time), assert 5 grouping pills render, click a pill (e.g. workflow), assert the pill gains aria-pressed="true", click again and aria-pressed="false".

2. **Within-cluster sort** — Change the dropdown selection from Newest first to Alphabetical; assert the store state via a data-attribute on the `<select>` (bind `value` attribute to reflect state, or read via locator). If direct store assertion isn't available from Playwright, inject a global like `window.__moshpitTest.filterStoreSnapshot()` for test-only use (guarded by `process.env.NODE_ENV !== 'production'` or a Vite define). If that's invasive, skip this check and rely on the visible selected option.

3. **Advanced disclosure** — Click the "Advanced filters" trigger, assert the collapsible content becomes visible (locator visibility check), click again to close, assert hidden. Add a chip via the `+ Add filter` popover's Advanced section (pick `cfg`), verify the chip appears inside the advanced row.

4. **Cluster overlay presence** — After gating, `page.locator('[data-testid="moshpit-cluster-overlay"]')` is visible. Do NOT assert exact bounding-box count or positions — those are Vitest's job (Plan 05).

Keep assertions to Locator actions + auto-retrying web-first assertions per Playwright best practices. No `waitForTimeout`. Mock asset API responses using project-specific types from `packages/ingest-types` if necessary (per AGENTS.md Playwright guidelines).

**Scope flexibility:** If an asset-ingestion mock is too expensive for this plan's scope, use Playwright to visit Moshpit on a bare page and assert only the UI chrome that renders independently of asset data: the grouping pills (always rendered when gated), the within-cluster dropdown, the Advanced disclosure trigger. Skip data-dependent assertions (cluster boxes only render when clusterTree is non-null, which requires real or mocked asset data).

Commit the spec even if it's minimal (D-21 explicitly says "may cover" — scope is discretionary).
  </action>
  <verify>
    <automated>pnpm exec playwright test --project=chromium --grep @moshpit --list 2>&1 | head</automated>
  </verify>
  <acceptance_criteria>
    - `browser_tests/tests/moshpit/lineage-groupings.spec.ts` exists
    - `grep -n "@moshpit" browser_tests/tests/moshpit/lineage-groupings.spec.ts` returns ≥1 hit
    - `grep -n "comfyPageFixture as test" browser_tests/tests/moshpit/lineage-groupings.spec.ts` returns 1 hit
    - `grep -n "waitForTimeout" browser_tests/tests/moshpit/lineage-groupings.spec.ts` returns 0 hits (forbidden per best practices)
    - `grep -n "grouping\|advanced\|within" browser_tests/tests/moshpit/lineage-groupings.spec.ts` returns ≥3 hits
    - `pnpm exec playwright test --project=chromium --grep @moshpit --list` exits 0 (lists the spec's tests without errors; running them is OPTIONAL for acceptance — D-21)
  </acceptance_criteria>
  <done>
    @moshpit Playwright spec exists covering grouping toggles + advanced disclosure + cluster overlay presence. Best-effort per D-21; real execution deferred to CI.
  </done>
</task>

<task type="auto">
  <name>Task 6: Write 04-HUMAN-UAT.md (D-20 dogfood checklist) + update 04-VALIDATION.md</name>
  <files>
    .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md,
    .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md
  </files>
  <read_first>
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-CONTEXT.md (D-20 scenario — 8 numbered steps; D-22 gate language)
    - .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md (current state — placeholder Per-Task Verification Map with TBD row; frontmatter nyquist_compliant: false)
    - .planning/phases/03-filter-sort-core-validation/03-VALIDATION.md (if present — pattern reference for how Phase 3 populated its verification map)
    - .planning/phases/03-filter-sort-core-validation/ (look for 03-HUMAN-UAT.md — if present, use as shape template)
  </read_first>
  <action>
**A. Create `.planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md`:**

Write the D-20 dogfood checklist as a numbered list, each step having:
- An instruction ("toggle workflow grouping alone")
- An expected observable result ("visible clusters match workflow filenames")
- A pass/fail checkbox

Include at the top:
- Phase reference + date
- Prerequisites (real ComfyUI backend at 127.0.0.1:8188 with ≥2 workflows worth of assets, ≥2 save nodes per workflow, overlapping models/prompts)
- Instructions for how to launch Moshpit and reach the filter gate

Include the 8-step scenario from D-20 verbatim:
1. Populate backend
2. Open Moshpit; apply filter (workflow: all, time: all)
3. Toggle workflow grouping alone — verify clusters match filenames, order by density desc
4. Add prompt grouping — verify nesting reorders automatically based on bucket sizes
5. Add saveNode + model + type groupings one at a time — verify depth-proportional spacing + (other) clusters
6. Change within-cluster sort — verify leaf order changes, cluster boundaries stable
7. Collapse/expand Advanced filters — verify added chips land in correct tier
8. Qualitative sign-off question: "Do clusters make comparable assets easier to find than a flat grid? Is auto-nesting predictable?" — Yes / Sort of / No

Add the D-22 gate statement at the end:
> **Gate (D-22):** If the qualitative sign-off is `No` or `Sort of`, PAUSE Phase 5. Phase 4 is the v3 Core Value validation milestone; negative sign-off means the lineage-grouping thesis needs rework before Tournament mode is built on top of it.

Include three signature blocks at the bottom: Tester name, Date, Sign-off verdict (Yes / Sort of / No), Notes.

Minimum 40 lines; max ~120 lines. Markdown only; no executable content.

**B. Update `.planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md`:**

Populate the Per-Task Verification Map with real rows. Replace the `TBD — planner fills` row with one row per task across all six plans. Match the format shown in the existing table header (Task ID | Plan | Wave | Requirement | Threat Ref | Secure Behavior | Test Type | Automated Command | File Exists | Status).

For each plan's tasks, a representative row looks like:
```
| 01-T1 | 01 | 1 | GROUP-02/03/05/06/07/08/CSORT-01 | — | Pure math, worker-safe, no network | unit | `pnpm test:unit -- src/platform/moshpit/services/groupAxes.test.ts --run` | ❌ W0 | ⬜ pending |
| 01-T2 | 01 | 1 | GROUP-02/08/10 | T-04-01-02 | Memoised bucket-key; <100ms @5k | unit | `pnpm test:unit -- src/platform/moshpit/services/clusterLayout.test.ts --run` | ❌ W0 | ⬜ pending |
| 02-T1 | 02 | 1 | GROUP-04 | — | Best-effort extraction; null on miss | unit | `pnpm test:unit -- src/platform/moshpit/services/paramNormalize.test.ts --run` | ✅ extend | ⬜ pending |
| 02-T2 | 02 | 1 | GROUP-04 | T-04-02-01 | Per-record try/catch; cursor streaming | unit | `pnpm test:unit -- src/platform/moshpit/services/thumbRepository.test.ts --run` | ✅ extend | ⬜ pending |
| 03-T1 | 03 | 2 | FILTER-12 | T-04-03-01 | Exhaustive-never default | unit | `pnpm test:unit -- src/platform/moshpit/services/filterMath.test.ts --run` | ✅ extend | ⬜ pending |
| 03-T2 | 03 | 2 | GROUP-01/CSORT-01 | — | Store-level state; no new trust boundary | unit | `pnpm test:unit -- src/platform/moshpit/stores/moshpitFilterStore.test.ts --run` | ✅ extend | ⬜ pending |
| 03-T3 | 03 | 2 | GROUP-01/09/CSORT-01 | T-04-03-02 | Memoised auto-nest computed | integration | `pnpm test:unit -- src/platform/moshpit/composables/useMoshpitFilteredAssets.test.ts --run` | ✅ extend | ⬜ pending |
| 03-T4 | 03 | 2 | GROUP-01/CSORT-01 | — | Mandatory deletion keeps typecheck green | build | `pnpm typecheck && [ ! -f src/platform/moshpit/components/MoshpitSortControls.vue ]` | ✅ existing | ⬜ pending |
| 04-T1 | 04 | 3 | GROUP-01 | T-04-04-01 | Interpolation-only rendering | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitGroupingToggles.test.ts --run` | ❌ W0 | ⬜ pending |
| 04-T2 | 04 | 3 | CSORT-01 | — | Native select; a11y via label for= | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitWithinClusterSort.test.ts --run` | ❌ W0 | ⬜ pending |
| 04-T3 | 04 | 3 | FILTER-12 | T-04-04-02 | Controlled Reka Collapsible | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitAdvancedFilters.test.ts --run` | ❌ W0 | ⬜ pending |
| 04-T4 | 04 | 3 | FILTER-12 | — | Tier-filtered chip rendering | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitFilterChipRow.test.ts src/platform/moshpit/components/MoshpitAddFilterPopover.test.ts --run` | ✅ extend | ⬜ pending |
| 05-T1 | 05 | 3 | GROUP-01/02 | T-04-05-01 | Template interpolation only | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitClusterOverlay.test.ts --run` | ❌ W0 | ⬜ pending |
| 06-T1 | 06 | 4 | GROUP-01/CSORT-01/FILTER-12 | — | Layout-order assertions | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitSettingsPanel.test.ts --run` | ✅ extend | ⬜ pending |
| 06-T2 | 06 | 4 | GROUP-01/02 | — | Overlay mount | component | `pnpm test:unit -- src/platform/moshpit/components/MoshpitCanvas.test.ts --run` | ✅ extend | ⬜ pending |
| 06-T3 | 06 | 4 | — (verification + i18n) | — | Portable [ ! -f ] existence tests + i18n removal | build | `[ ! -f src/platform/moshpit/components/MoshpitSortControls.vue ] && pnpm typecheck && pnpm knip` | — | ⬜ pending |
| 06-T4 | 06 | 4 | FILTER-12 | — | Label migration | component/build | `pnpm typecheck` | ✅ existing | ⬜ pending |
| 06-T5 | 06 | 4 | GROUP-01/FILTER-12 | — | @moshpit UI smoke | e2e | `pnpm exec playwright test --project=chromium --grep @moshpit` | ❌ W0 | ⬜ pending |
| 06-T6 | 06 | 4 | All (gate) | — | D-22 qualitative gate | manual | see 04-HUMAN-UAT.md | ❌ W0 | ⬜ pending |
```

(Adjust Requirement columns per actual REQ-ID coverage; the above is illustrative.)

Flip frontmatter:
Flip frontmatter (yaml block in VALIDATION.md):
`phase: 04`, `slug: lineage-groupings-within-cluster-sort`, `status: draft`, `nyquist_compliant: true` (was false — now filled), `wave_0_complete: false` (stays false until executor creates W0 test files), `created: 2026-04-21`

Update the "Wave 0 Requirements" checklist to match the actual new-file list (drop old paths like `src/workbench/moshpit/...` — actual path is `src/platform/moshpit/...`).
  </action>
  <verify>
    <automated>test -f .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md && grep -q "nyquist_compliant: true" .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md && echo OK</automated>
  </verify>
  <acceptance_criteria>
    - `test -f .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md` succeeds
    - `wc -l < .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md` ≥ 40
    - `grep -n "D-22\|pause Phase 5\|PAUSE Phase 5" .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md` returns ≥1 hit
    - `grep -n "Sign-off\|sign-off" .planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md` returns ≥1 hit
    - `grep -n "nyquist_compliant: true" .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md` returns 1 hit
    - `grep -n "nyquist_compliant: false" .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md` returns 0 hits
    - `grep -n "TBD — planner fills" .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md` returns 0 hits
    - `grep -cE "^\\| 0[1-6]-T[0-9]+ \\|" .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md` ≥ 14 (count of real task rows across the 6 plans)
    - `grep -n "src/platform/moshpit/" .planning/phases/04-lineage-groupings-within-cluster-sort/04-VALIDATION.md` returns ≥6 hits (real paths, not the old src/workbench/ paths)
  </acceptance_criteria>
  <done>
    HUMAN-UAT dogfood checklist exists with the 8-step scenario + D-22 gate; VALIDATION.md's per-task map is real and nyquist_compliant is flipped to true.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 7: Human verification — Phase 4 integration + HUMAN-UAT gate (D-22)</name>
  <files>.planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md</files>
  <what-built>
    All of Phase 4:
    - Lineage grouping math (Plan 01)
    - saveNodeIdentity + IDB v2→v3 migration (Plan 02)
    - Store + composable refactor + deprecated sort-UI deletion (Plan 03)
    - Settings-panel UI: grouping pills, within-cluster sort, advanced disclosure, saveNode filter (Plan 04)
    - Cluster overlay on canvas (Plan 05)
    - Settings panel + canvas composition, i18n cleanup, Playwright spec, HUMAN-UAT doc (Plan 06)
  </what-built>
  <how-to-verify>
    1. Start the backend (`conda activate comfyui && cd /Users/willie/Documents/projects/comfy/ComfyUI && python main.py`) and the frontend (`pnpm dev`).
    2. Navigate to http://localhost:5173/ and open Moshpit.
    3. Run through every step of `.planning/phases/04-lineage-groupings-within-cluster-sort/04-HUMAN-UAT.md` in order.
    4. Record pass/fail per step in the file's checkboxes.
    5. At the qualitative sign-off question (step 8), answer: **Yes / Sort of / No**.
    6. Fill in the signature block (Tester name, Date, Verdict, Notes).

    **Important: D-22 gate.** If verdict is `No` or `Sort of`:
    - Do NOT mark Phase 4 complete.
    - Do NOT proceed to Phase 5 planning.
    - Return here with notes so Phase 4 can be gap-closed or the v3 pivot reconsidered.

    If verdict is `Yes`:
    - Mark Phase 4 complete.
    - ROADMAP.md Phase 4 checkbox to [x].
    - Proceed to Phase 5 planning when ready.
  </how-to-verify>
  <action>Pause execution and present the how-to-verify instructions above to the user. Wait for the resume-signal before proceeding.</action>
  <verify>Human confirms via the resume-signal. No automated verification — this task IS the D-22 qualitative gate.</verify>
  <done>User has typed the resume-signal with a UAT verdict (Yes / Sort of / No). Verdict recorded in 04-HUMAN-UAT.md + 04-06-SUMMARY.md.</done>
  <resume-signal>Type "approved (UAT verdict: Yes)" or "blocked (UAT verdict: Sort of | No)" with notes</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

No new boundaries. This plan composes existing components and finalises the i18n cleanup for the sort→grouping migration.

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-04-06-01 | Denial of Service | Incomplete i18n key removal leaves dangling `t('moshpit.sort.xxx')` calls | mitigate | Task 3 acceptance criterion greps the entire `src/` tree for `moshpit.sort` — must return 0 hits before the task completes. Task 4 additionally greps `MoshpitGridSpacingControl.vue` explicitly. |
| T-04-06-02 | Information Disclosure | HUMAN-UAT leaks user's real workflow filenames to the checklist | accept | The user is the tester; they choose which workflows to exercise. No automated telemetry, no upload — the checklist lives in the local repo only. |
| T-04-06-03 | Tampering | Playwright spec depends on dev server state | mitigate | Spec uses `comfyPageFixture` which brings its own per-test isolation. Asset-dependent assertions are minimised per Task 5's scope flexibility note. |
</threat_model>

<verification>
```bash
pnpm test:unit --run   # full moshpit unit + component suite
pnpm typecheck
pnpm lint
pnpm knip
node -e "JSON.parse(require('fs').readFileSync('src/locales/en/main.json'))"
pnpm exec playwright test --project=chromium --grep @moshpit --list   # confirms spec parses
```
</verification>

<success_criteria>
- Settings panel composition matches D-18 + CONTEXT Integration Points order
- MoshpitClusterOverlay mounted parallel to the sprite layer (in whichever parent actually owns the sprite layer — MoshpitCanvas.vue or MoshpitView.vue per Plan 03 SUMMARY note)
- MoshpitSortControls + MoshpitAxisOverlay files remain absent (deleted by Plan 03 Task 4; verified here with portable `[ ! -f ]` tests)
- Zero `moshpit.sort` references remain in src/
- MoshpitGridSpacingControl label references `moshpit.grouping.spacingLabel`
- `@moshpit` Playwright spec exists and parses
- 04-HUMAN-UAT.md exists with the D-20 scenario + D-22 gate
- 04-VALIDATION.md has real per-task rows + `nyquist_compliant: true`
- Full `pnpm typecheck + pnpm lint + pnpm knip + pnpm test:unit` green
- Human sign-off recorded per D-22
</success_criteria>

<output>
Create `.planning/phases/04-lineage-groupings-within-cluster-sort/04-06-SUMMARY.md` noting:
- Final Settings panel layout order (list in order, one per line)
- Parent component that received the MoshpitClusterOverlay mount (MoshpitCanvas.vue or MoshpitView.vue)
- Confirmation that Plan 03 Task 4 deletions still hold (absence tested portably)
- i18n keys removed in this plan (moshpit.sort.*)
- HUMAN-UAT verdict (Yes / Sort of / No) + verdict date
- D-22 outcome: whether Phase 5 is unblocked or paused
- Any follow-up issues surfaced during dogfood (e.g. custom save-node surfacing (Pitfall 3), cluster-overlay label culling needs (RESEARCH OQ 4), multi-output workflow save-node mismatch)
</output>
</content>
